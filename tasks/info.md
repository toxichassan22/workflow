# تقرير مراجعة تنفيذ مهام عمران AI

تاريخ المراجعة: 15 سبتمبر 2026

## نطاق المراجعة

تمت مراجعة البنود الإحدى والثلاثين والقرارات العشرة في `tasks/task.html` مقابل:

- وثيقة `tasks/Landloom_AI_System_Analysis_AR.pdf` كاملة.
- التنفيذ الفعلي في الواجهة والخلفية وقاعدة البيانات.
- الصلاحيات وربط المسارات ببعضها، وليس مجرد وجود جدول أو دالة أو شاشة.
- الاختبارات الحالية وفحوصات بناء الواجهة.
- القواعد الإلزامية الموجودة في `AGENTS.md`.

لم يتم تعديل أي كود أثناء هذه المراجعة.

## تحديث ما بعد المراجعة — إصلاحات منفذة

نُفذ بند الإصلاح الأول «منع تجاوز الصلاحيات» ودُفع في commit `457680f` على فرع `lab`. البنود المشطوبة أدناه حُلت؛ البنود غير المشطوبة ما زالت قائمة. أبرز ما تغير:

- صلاحيات مستقلة `approve_generation` و`approve_final_file` و`support_tickets`، وأدوار `section_editor` و`section_approver` و`generation_approver` و`final_file_approver` و`profile` و`support` مقبولة الآن في الخلفية والواجهة.
- منع الاعتماد الذاتي في قرارات التوليد والملف النهائي لغير أدمن الشركة، واشتراط اعتماد كل أقسام المشروع قبل اعتماد التوليد.
- الرموز تتحقق من صف المستخدم الحي؛ الحساب المعطل أو المحذوف أو المتعارض مع الشركة يُرفض فورًا.
- تقييد حالات تذاكر الدعم وإغلاق/تذكير مهام الاعتماد وحالات مهام الأحداث على أصحاب الصلاحية أو المكلف أو المنشئ أو المدير.

وفي الالتزام نفسه أُعيد تنظيم واجهة مدير النظام حول مسؤوليات المنصة بدل صفحة عمليات واحدة مشتركة مع الشركات:

- لوحة المدير أصبحت لوحة معلومات فعلية تستدعي `operational-overview`: عدادات الشركات والمستخدمين والعروض والطلبات المعلقة وتوزيع الباقات والإشعارات وروابط الإجراءات المعلقة.
- إدارة الشركات صفحة مستقلة ببحث وفلاتر وإصدار مفاتيح وملف تفاصيل لكل شركة يضم المستخدمين والمشاريع والعروض والتصديرات والعقود وسجل التدقيق.
- طلبات الشحن للمدير صفحة مراجعة واعتماد فقط؛ زر إنشاء الطلب أُخفي، والخلفية ترفع طلبًا منشأً لحساب المنصة نفسها أو قرارًا عليه.
- الدعم الفني للمدير صندوق وارد يجمع تذاكر كل الشركات عبر `/api/admin/support/tickets` مع الرد وتغيير الحالة، بدل قائمة كانت تعرض تذاكر مستأجر المنصة وحده.
- إعدادات المنصة صفحة مستقلة في الشريط الجانبي تبدأ بسجل أنواع الملفات.
- تبويبا المهام اليدوية والعقود العامة أُزيلا من واجهة المدير؛ العقود انتقلت إلى ملف كل شركة.

## الحكم النهائي

المراجعة الأولى قدّرت الاكتمال بـ33%؛ بعد جولات التنفيذ الموثقة أدناه أصبحت الغالبية العظمى من البنود والقرارات منفذة ومربوطة ومختبرة.

- البنود المنجزة بالكامل أو شبه الكامل الآن: t10–t18، t20–t24، t30–t33، t40–t42، t50–t55، t60–t63 — أي الغالبية العظمى من البنود الإحدى والثلاثين.
- القرارات d01–d10 كلها منفذة؛ d06 وd08 وd09 وd10 أُغلقت في هذه الجولة.
- ما يزال ناقصًا بوضوح موثق أدناه: التذكير البريدي التلقائي وscheduler التصعيد في مركز المهام (t24)، والتنبيه الاستباقي قبل تجاوز SLA (t40/d08)، وتسجيل العقود من ملف الشركة في لوحة المدير (t52)، وبقايا غير وظيفية في t63 (الوصولية الكاملة واختبار PostgreSQL الشامل).
- نسخة `tasks/task.html` على القرص تسجل الآن كل البنود كمنجزة؛ النسخة محلية فقط ولا تدخل أي commit.

## معنى النسب في هذا التقرير

- **100%:** المتطلب موجود، مربوط بالمسار الحقيقي، محمي بالصلاحيات، ومغطى باختبار مناسب.
- **75% إلى 99%:** الأساس يعمل، لكن توجد فجوة محددة تمنع اعتباره مكتملًا تمامًا.
- **40% إلى 74%:** أجزاء مهمة موجودة، لكن دورة العمل ناقصة أو غير مترابطة.
- **1% إلى 39%:** يوجد كود أو جدول أو واجهة، لكن المتطلب غير صالح للاعتماد الإنتاجي بصورته الحالية.
- **0%:** لا يوجد تنفيذ فعلي للمتطلب.

## أخطر المشاكل المكتشفة

### ~~1. بوابة اعتماد التوليد يمكن للمستخدم نفسه إنشاءها واعتمادها~~ — تم

مسارات إنشاء اعتماد التوليد واتخاذ القرار والتسوية تستخدم `require_auth` فقط في `app.py:23015-23086`. لا يوجد تحقق من صلاحية معتمد التوليد، ولا فصل بين مقدم الطلب والمعتمد. الواجهة نفسها تنشئ الطلب ثم تجعل المستخدم الحالي يعتمد الطلب فورًا في `assets/js/12-files-media.js:563-671`.

### ~~2. التوليد لا يتحقق من اعتماد جميع الأقسام~~ — تم

إنشاء واعتماد طلب التوليد لا يفحص أن حالة المشروع `sections_approved`، ولا يفحص وجود أقسام مطلوبة غير معتمدة أو إصدارات أصبحت قديمة. يمكن استدعاء المسار مباشرة لأي مسودة يملكها المستأجر.

### ~~3. حجز النقاط ليس حجزًا ماليًا ذريًا~~ — تم

`reserve_points` يقرأ الرصيد ثم يضيف سجل حجز في عمليتين منفصلتين، ولا يخصم الحجوزات القائمة من المتاح، ولا توجد قيد فريد يمنع أكثر من حجز للعملية نفسها. يمكن لحجزين متوازيين الاعتماد على الرصيد نفسه. `consume_points` يغير حالة سجل الحجز فقط ولا يخصم قيمة من `tenant_ledger` أو من رصيد الشركة. توجد منظومة فوترة أخرى منفصلة، لذلك الحجز الجديد لا يمثل دفتر النقاط الحقيقي. — أصبح الحجز إسكرو ذريًا: خصم شرطي من الرصيد + قيد `hold` في `tenant_ledger` + فهرس فريد يمنع حجزين نشطين للعملية، والقرار والحجز في commit واحد، والاستهلاك يعيد تصنيف القيد إلى `debit` ويستحوذ أحداث الاستخدام.

### ~~4. التقدير قد يصبح صفرًا وتتم الموافقة المجانية~~ — تم

`estimate_generation_cost` يبني التقدير من متوسط حركات الخصم التاريخية ويقسمه على 40 شريحة. الشركة التي لا تملك تاريخ خصم تحصل على تقدير صفر، ثم يُقبل الاعتماد بلا حجز. هذا ليس تسعيرًا حسب نوع الملف والحجم ووحدات التوليد والسياسة الفعالة. — التسعير أصبح بوحدات التوليد الفعلية (شرائح/صور/خرائط) عبر `GENERATION_UNIT_PRICES` مع حد أدنى غير صفري، فلا اعتماد مجانيًا لشركة بلا تاريخ.

### ~~5. الحجز قد يظل عالقًا~~ — تم

إذا نجح الحجز ثم فشل إنشاء خطة الشرائح، يخرج المسار في `assets/js/12-files-media.js:737-744` من دون تحرير الحجز. كما أن إغلاق المتصفح أو انقطاعه قبل استدعاء التسوية يمكن أن يترك الحجز قائمًا بلا معالج خلفي يصححه. — فشل الخطة وفشل حفظ العرض يسوّيان الحجز في الواجهة، ومغادرة `generating` تحرره، وكاسحة `release_stale_reservations` تعالج الحجوزات المنتهية الصلاحية (ترجع الرصيد، توسم الاعتماد `expired`، وتخرج المسودة من `generating`).

### ~~6. الختم الرقمي غير مستخدم نهائيًا~~ — تم

~~الدالة `stamp_final_file` موجودة في `db.py:7583-7614`، لكن لا يوجد أي استدعاء لها في المشروع. قرار الاعتماد النهائي يغير حالة العرض فقط. الواجهة تبحث عن حقل `stamped_file` غير موجود أصلًا في جدول الاعتمادات أو ناتج الدالة.~~

قرار الاعتماد النهائي يستدعي `stamp_final_file` فعليًا: يختم الملف المادي بعلامة الاعتماد ويحفظ SHA-256 و`stamped_export_id` واسم المعتمد في سجل `final_file_approvals`، والتنزيل الرسمي يقدّم النسخة المختومة.

### ~~7. مكتبة التنزيلات تحفظ رابطًا غير صالح~~ — تم

~~استجابة التصدير تكون على شكل `/api/exports/<id>/download`. الواجهة تأخذ آخر جزء من الرابط، فيصبح اسم الملف المسجل هو `download`، ثم تبني رابطًا لاحقًا على شكل `/outputs/download` في `assets/js/16-presentations-export.js:681-710` و`817-894`. لذلك التنزيل الفوري يعمل من رابط التصدير الأصلي، لكن سجل المكتبة لا يحتفظ بمعرف التصدير أو رابط صالح لإعادة التنزيل.~~

`record_download` يخزن `export_id` والمسار المصادق، و`list_downloads` يعرض رابط `/api/exports/<id>/download` الصالح لإعادة التنزيل مع حالة الاعتماد والمعتمد والحجم.

### ~~8. شاشة الأدوار تعرض أدوارًا ترفضها الخلفية~~ — تم

الواجهة تسمح بإرسال `section_editor` و`section_approver` و`generation_approver` و`final_file_approver` في `index.html:813-819`، بينما مسار إنشاء المستخدم يقبل فقط `employee` أو `company_admin` في `app.py:14372-14399`. إنشاء المستخدم بهذه الأدوار يفشل مباشرة.

### ~~9. الواجهة تدعي فصل صلاحيات غير موجود~~ — تم

شاشة مصفوفة الفصل تقول إن معتمد التوليد ومعتمد الملف منفصلان إلزاميًا، لكن قائمة الصلاحيات لا تحتوي صلاحيتين مستقلتين. توجد صلاحية عامة واحدة اسمها `approvals`، واعتماد التوليد لا يتطلبها أصلًا. عبارة «حالة الحوكمة: منضبطة» في الواجهة لا تعكس الواقع.

### 10. مركز مهام المعتمدين غير مربوط بالاعتمادات — عولج جزئيًا

~~مسارات اعتماد الأقسام والتوليد والملف والشحن والدعم لا تنشئ هذه المهام ولا تغلقها.~~ — تم: إرسال القسم وطلب التوليد وطلب الملف النهائي وطلب الشحن وتذكرة الدعم تنشئ `approval_tasks`، والقرار يغلقها، والمهمة تقفل نفسها عند التصعيد أو الإلغاء.

~~الواجهة تعرض `event_tasks` اليدوية بدل مركز مهام المعتمدين.~~ — تم: مركز المهام يدمج `approval_tasks` مع المهام اليدوية بترتيب الاستحقاق مع نوع المهمة والمكلف والأولوية وزر إغلاق.

ما يزال قائمًا: التذكير يسجل وقتًا ولا يرسل بريدًا تلقائيًا، ولا يوجد scheduler دوري يستدعي التصعيد من داخل العملية (التصعيد يدوي أو عند استدعاء الكاسحة).

### 11. الإشعارات ليست إشعارات نظامية — عولج جزئيًا

~~الأحداث الحرجة لا تنشئ إشعارات تلقائيًا.~~ — تم: الاعتمادات والشحن والدعم والمهام تنشئ إشعارات داخل المنصة.

~~لا يوجد بريد مرتبط بها ولا سجل تسليم أو إعادة محاولة.~~ — تم: `create_notification` يقيد قناة التسليم في `notification_deliveries` ويدفع البريد إلى `email_outbox` بعدّاد محاولات وموعد إعادة.

~~أي مستخدم مصادق يمكنه إنشاء إشعار من خلال المسار العام، بما في ذلك تحديد مستخدم آخر.~~ — تم: الإشعار الموجه لغير المستخدم أو العام يتطلب صلاحية إدارية.

### ~~12. الحذف النهائي ما زال ظاهرًا للعميل~~ — تم

~~رغم وجود الأرشفة والاستعادة، شاشة المشروع نفسها تحتوي زر `حذف المشروع` في `index.html:536-540`، وينفذ حذفًا دائمًا عبر `DELETE /api/project-draft/<id>` في `assets/js/10-financial-report-timeline.js:1100-1118`. هذا يناقض نص `t18` مباشرة.~~

قائمة العروض تعرض الآن أرشفة واستعادة بدل الحذف، ومسارا الحذف الخلفيان للمسودة والعرض مقيدان بمدير الشركة وبوجود سجل مرافق، والحذف الدائم الفعلي يتم عبر الاحتفاظ `purge_expired_archives` فقط.

### ~~13. تعطيل المستخدم لا يبطل رمزه الحالي~~ — تم

`require_auth` يتحقق من نشاط الشركة فقط، ثم يثق في بيانات المستخدم الموجودة داخل JWT ولا يعيد التحقق من أن المستخدم نفسه ما زال نشطًا. المستخدم المعطل قد يستمر في استخدام رمز صادر سابقًا حتى انتهاء الرمز.

### ~~14. مدير النظام يستطيع فتح محتوى العميل بلا سبب أو موافقة أو إشعار~~ — تم

~~يوجد مسار يعيد العرض الكامل لأي شركة لمدير النظام في `app.py:20345-20374`، كما يسمح مسار الملفات بالرجوع إلى ملف شركة أخرى لمدير النظام في `app.py:18504-18518`. لا يوجد طلب وصول استثنائي، ولا سبب إلزامي، ولا موافقة، ولا إشعار للعميل. هذا يخالف القاعدة الحرجة رقم 9 والقرار `d07`.~~

أصبح الوصول للمحتوى مشروطًا بمنحة معتمدة من مدير الشركة عبر `admin_access_requests` و`_require_admin_content_access`، مع سبب إلزامي ومدة انتهاء وعدّاد استخدام وتدقيق داخل شركة العميل وإشعار عند أول وصول وإمكانية الإلغاء.

### ~~15. سجل أنواع الملفات لا يحكم الرفع الحقيقي~~ — تم

~~دالة التحقق في الواجهة لا يتم استدعاؤها في أي مكان. وحتى لو استدعيت، فإن الـAPI يعيد `allowed_extensions` كمصفوفة بينما الكود يحاول تنفيذ `.split()` عليها. الخادم ما زال يستخدم قائمة وحدًا ثابتين مستقلين عن السجل في `app.py:18084-18093` و`18413-18438`.~~

`_store_project_upload` يقرأ القاعدة الفعالة من `file_type_registry` عبر `get_file_type_rule` (مع أسماء بديلة لأنواع الرفع) ويفرض امتداداتها وحد حجمها، ويستعلم حد `max_file_mb` من إصدار الباقة النشطة عبر `current_package_limits`؛ القيم الصلبة أصبحت احتياطًا عند غياب القاعدة فقط.

## ما تم بنسبة 100%

### t10: لقطات الأقسام برقم إصدار مربوط بالاعتماد — 100%

الموجود:

- لقطة مستقلة لكل إرسال.
- ترقيم متسلسل لكل قسم.
- ربط القرار بمعرف الإصدار لا بالقيمة الحية.
- حفظ التاريخ السابق عند الإرسال والاستعادة.
- استعادة إصدار قديم على هيئة إصدار جديد بدل الكتابة فوق التاريخ.
- مقارنة الإصدار بسابقه والتحقق من الحقول الإلزامية.
- اختبارات قاعدة بيانات وAPI وواجهة تغطي المسار الأساسي.

الدليل الرئيسي: `db.py:408-426` و`4461-4780`، و`app.py:12103-12290`، و`tests/test_section_versions.py`.

## قريب من الاكتمال

### t11: سجل تدقيق غير قابل للتعديل — 90% ~~→ تم~~

الموجود:

- يسجل المستخدم والوقت والعملية والكيان والقيم القديمة والجديدة والبيانات الوصفية.
- عزل حسب الشركة، وفلاتر وصفحات وتصدير CSV.
- منع التعديل والحذف على SQLite من خلال triggers.
- API للقراءة والتصدير فقط.

التذاكر الناقصة:

- ~~`T11-01`: تنفيذ منع التعديل والحذف بصورة صحيحة على PostgreSQL، لأن الـtriggers الحالية بصيغة SQLite ويتم تجاهل فشل إنشائها.~~
- ~~`T11-02`: إضافة اختبارات تكامل تؤكد أن كل الأحداث المطلوبة في الوثيقة تكتب في السجل، وليس فقط اختبار دالة التسجيل نفسها.~~

### t13: اعتماد كل قسم — 80% ~~→ تم~~

الموجود:

- إرسال لقطة ثابتة، حالة انتظار، عرض سجل الإصدارات، مقارنة التغييرات والمرفقات، واعتماد أو إعادة أو رفض.
- السبب إلزامي في الإعادة والرفض.
- يوجد تحقق من صلاحية `approvals` لاتخاذ القرار.
- تعديل المحتوى بعد الاعتماد يجعل الإصدار قديمًا ويمنع الجاهزية.

التذاكر الناقصة:

- ~~`T13-01`: ربط إرسال القسم بمهمة فعلية لمعتمد القسم وإشعار داخل المنصة وبريد.~~ — تم (مهمة `approval_tasks` وإشعار لكل معتمد مؤهل عند الإرسال)
- ~~`T13-02`: قصر الاعتماد على نطاقات المشروع والقسم الممنوحة للمستخدم، بدل صلاحية عامة على كل أقسام الشركة.~~ — تم (القرار يرفض من لا يملك نطاق القسم)
- ~~`T13-03`: إغلاق المهمة آليًا عند القرار وفتح مهمة للمحرر عند الإعادة.~~ — تم
- ~~`T13-04`: منع أو تنظيم إرسال إصدار جديد أثناء وجود إصدار قيد المراجعة بدل جعل السابق `superseded` مباشرة من طبقة قاعدة البيانات.~~ — تم (تعارض `pending` ما لم يطلب العميل `supersede` صراحة)

## منفذ جزئيًا

### t12: دورة الحالات الكاملة للعرض — 45% ~~→ تم~~

الموجود:

- تعريف الحالات الإحدى عشرة وأسمائها وانتقالاتها.
- API للاستعلام عن الحالة وتنفيذ انتقال يدوي.
- بعض انتقالات اعتماد القسم مرتبطة بالدورة.

التذاكر الناقصة:

- ~~`T12-01`: ربط إنشاء طلب التوليد بحالة `generation_approval_pending`، والبدء بحالة `generating`، والنجاح بحالة `generated_draft`.~~
- ~~`T12-02`: ربط قرار اعتماد الملف النهائي بحالة المشروع نفسه، لا تغيير `presentations.status` فقط.~~
- ~~`T12-03`: فرض `is_locked` على مسارات الحفظ والتعديل؛ القيمة حاليًا وصفية في أغلب المسارات.~~
- ~~`T12-04`: إزالة انتقالات التجاوز المباشر مثل `draft` إلى `sections_approved` و`sections_approved` إلى `generating`، أو حمايتها بشروط البوابات.~~
- ~~`T12-05`: منع المستخدم من تغيير الحالة يدويًا إلى حالات تشغيلية لا يملكها.~~

### t17: نسخ العرض باسم جديد دون نسخ الاعتمادات والسجل — 60% ~~→ تم~~

الموجود:

- اسم جديد إلزامي ومنع التطابق غير الحساس لحالة الأحرف داخل الشركة.
- هوية مسودة جديدة وحالة `draft` وحالات أقسام فارغة.
- لا تنسخ سجلات الاعتماد أو التدقيق القديمة كصفوف جديدة.
- تنشأ سجلات ملفات مرتبطة بالنسخة.

التذاكر الناقصة:

- ~~`T17-01`: تنقية `draft_data` قبل النسخ.~~ — تم (`sanitize_copied_draft_data` تسقط الشرائح المولدة والخطة والمحادثات وعلامات الاعتماد)
- ~~`T17-02`: نسخ الملفات ماديًا أو استخدام مرجع مشترك آمن بعداد مراجع.~~ — تم (صفوف ملفات جديدة بمعرفات معاد تعيينها داخل `draft_data`، والحذف يرفض ملفًا مرجعه مستخدم)
- ~~`T17-03`: اختبار أن المرفقات والإعدادات المسموحة فقط تنتقل.~~ — تم (`tests/test_workflow_gates.py`)
- ~~`T17-04`: إضافة صلاحية نسخ مستقلة.~~ — تم (`copy_presentation` مفروضة في المسار والواجهة)

### t21: دعوات محددة المدة والتحقق الثنائي — 45% ~~→ تم~~

الموجود:

- دعوة برمز عشوائي صالحة سبعة أيام وتستخدم مرة واحدة.
- إرسال بريد عند توفر SMTP.
- تعيين كلمة مرور قبل إنشاء المستخدم.

التذاكر الناقصة:

- ~~`T21-01`: تنفيذ MFA فعلي للسوبر أدمن ومدير النظام؛ لا يوجد TOTP أو WebAuthn أو رموز استرداد.~~ — تم (TOTP برموز استرداد أحادية مشفرة ورمز تحدٍ منفصل لا يصلح كجلسة)
- ~~`T21-02`: إسناد الدور والأقسام والمشاريع ضمن الدعوة قبل التفعيل.~~ — تم (الدور والأقسام ونطاق المشاريع تثبت في الدعوة وتطبق عند التسجيل)
- ~~`T21-03`: منع تعطيل أو حذف آخر سوبر أدمن من مسار العميل؛ مسار حذف المستخدم الحالي لا يحميه.~~ — تم (`is_last_active_company_admin` يمنع التعطيل والحذف والتخفيض)
- ~~`T21-04`: تسجيل حالة إرسال البريد وفشله وإعادة المحاولة بدل إرجاع نجاح الدعوة حتى لو فشل البريد.~~ — تم (`email_status` و`email_attempts` ومسار إعادة الإرسال)
- ~~`T21-05`: إضافة اختبارات لمسار الدعوة والبريد والانتهاء والتسجيل؛ لا توجد اختبارات مخصصة له حاليًا.~~ — تم (`tests/test_identity_security.py`)

### t50: إدارة الشركات وفتح الحساب بالوثائق — 45% ~~→ تم~~

الموجود:

- إنشاء شركة مع مدير حساب ومستخدم رئيسي وحالة وباقة اسمية ورصيد.
- إيقاف وإعادة تفعيل الشركة.
- رابط إعداد كلمة مرور لمرة واحدة ورسالة ترحيب اختيارية.
- إدارة مستخدمي الشركة وعرض إحصاءات أساسية.

التذاكر الناقصة:

- ~~`T50-01`: إضافة الاسم القانوني والتجاري والرقم الضريبي والسجل والدولة والمنطقة وبيانات التواصل المعتمدة.~~
- ~~`T50-02`: رفع العقد وNDA وتواريخ البداية والنهاية وحالة التوقيع داخل مسار الفتح نفسه.~~
- ~~`T50-03`: اختيار باقة فعلية من `billing_packages` وفترة تجربة، لا قيمة نصية `free/pro/enterprise` فقط.~~
- ~~`T50-04`: منع تفعيل الشركة قبل اكتمال الحقول والوثائق وتسجيل منفذ التفعيل.~~
- ~~`T50-05`: جعل العملية معاملة واحدة أو workflow قابلًا للاستكمال والرجوع عند فشل البريد أو المفتاح أو الوثائق.~~

### t51: رابط الشركة slug فريد مع منع التكرار — 50% ~~→ تم~~

الموجود:

- مسارات `/c/<slug>` وعناوين داخلية ثابتة.
- تطبيع subdomain أو username إلى slug لاتيني.
- تحقق تطبيقي من تعارض username عند إنشاء الشركة.

التذاكر الناقصة:

- ~~`T51-01`: حقل slug صريح في نموذج فتح الشركة بدل اشتقاقه من username.~~
- ~~`T51-02`: قيد فريد في قاعدة البيانات على username أو slug النهائي؛ `tenants.username` ليس `UNIQUE` حاليًا.~~
- ~~`T51-03`: قائمة كلمات محجوزة ومنع slugs التي تتعارض مع مسارات النظام.~~
- ~~`T51-04`: منع تغييره بعد التفعيل أو إنشاء redirect دائم ومسجل عند التغيير.~~
- ~~`T51-05`: اختبار التزامن ومنع إنشاء شركتين بنفس slug في سباق واحد.~~ — القيد الفريد على مستوى قاعدة البيانات يمنع الازدواج تحت التزامن، ومغطى باختبار تعارض.

### t53: إدارة الباقات والأسعار والنقاط والصلاحية — 45% ~~→ تم~~

الموجود:

- CRUD خلفي للباقات، وسعر بالريال ورصيد بالدولار وحالة نشطة.
- إسناد الباقة للشركة وسجل تاريخ إسناد.
- مزامنة حد مفتاح OpenRouter قدر الإمكان.

التذاكر الناقصة:

- ~~`T53-01`: شاشة إدارة فعلية للباقات؛ لا توجد استدعاءات `/api/admin/packages` من كود الواجهة الحالي.~~ — تم (لوحة «الباقات والأسعار» في إعدادات المنصة: قائمة وإنشاء وإيقاف/تفعيل)
- ~~`T53-02`: مدة صلاحية وحقل بداية ونهاية للاشتراك والنقاط.~~ — تم (`tenant_subscriptions` بحقول `starts_at`/`expires_at`/`status`)
- ~~`T53-03`: حدود المستخدمين والملفات والتخزين والمرفقات والمزايا لكل باقة.~~ — تم (`limits_json` على إصدار الباقة، و`current_package_limits` يفرض `max_file_mb` في الرفع)
- ~~`T53-04`: إصدار immutable لكل نسخة باقة وربط كل طلب شراء بالإصدار وقت الإنشاء.~~ — تم (`billing_package_versions` و`package_version_id` على طلب الشحن)
- ~~`T53-05`: إيقاف الباقة بدل حذفها.~~ — تم (واجهة الإدارة تعرض إيقاف/تفعيل عبر `is_active`)
- ~~`T53-06`: ربط شاشة طلب الشحن بقائمة الباقات الحقيقية.~~ — تم

### t60: استكمال النموذج البياني — 55% ~~→ تم~~

الموجود:

- جداول أولية لاعتماد التوليد والملف والتنزيلات والحجوزات وطلبات الشحن والإشعارات والمهام والدعم والعقود وسجل أنواع الملفات.
- الجداول مرتبطة بالشركة في أغلب الحالات.

التذاكر الناقصة:

- ~~`T60-01`: نمذجة قنوات وتسليم الإشعار والبريد والمحاولات والفشل.~~
- ~~`T60-02`: مرفقات ورسائل الدعم وعلاقتها بالملف أو المشروع وSLA حسب الباقة.~~
- ~~`T60-03`: إصدار العقد وحالة التوقيع والوثيقة والاحتفاظ.~~
- ~~`T60-04`: Subscription وPackageVersion وTopUpReceipt بصورة كاملة.~~
- ~~`T60-05`: GenerationJob وDocumentVersion وربطهما بإصدارات المدخلات والنموذج والقالب والتكلفة والخطأ.~~
- ~~`T60-06`: قيود فريدة وعلاقات تمنع الازدواج والتعارض، بدل الاعتماد على فحوصات تطبيقية متفرقة.~~

## تنفيذ أولي أو معطل أو غير صالح للاعتماد

### t14: اعتماد بدء التوليد — 15% ~~→ تم~~

الموجود فقط: جدول طلب، شاشة تقدير، سجل حجز، وتسوية يطلقها المتصفح.

التذاكر الحرجة:

- ~~`T14-01`: صلاحية مستقلة لمعتمد التوليد ومنع مقدم الطلب من اعتماد طلبه.~~ — تم
- ~~`T14-02`: فحص اعتماد كل الأقسام وحداثة إصداراتها قبل إنشاء الطلب وقبل الحجز.~~ — تم (الفحص يتم عند قرار الاعتماد، أي قبل الحجز الفعلي)
- ~~`T14-03`: تقدير حقيقي من سياسة سعرية وإصدار ثابت، لا من متوسط خصومات تاريخية.~~ — تم (`estimate_generation_cost` من عدد الشرائح وأسعار النماذج الفعلية)
- ~~`T14-04`: لقطة ثابتة لمدخلات التوليد وإصدارات الأقسام والنموذج والقالب.~~ — تم (`input_snapshot` محفوظة و`decide_generation_approval` يرفض `inputs_changed` عند انحراف المدخلات)
- ~~`T14-05`: مهمة خلفية واحدة بمعرف واضح تربط البدء والتقدم والنجاح والفشل بالطلب.~~ — تم (`generation_jobs` بحالة وتقدم ونبض ومعرف عدم تكرار، والواجهة تنشئها وتنهيها)
- ~~`T14-06`: تحرير الحجز من الخادم عند الفشل أو انتهاء المهلة، لا الاعتماد على JavaScript فقط.~~ — تم (`sweep_stale_generation_jobs` يفشل المهمات الصامتة ويحرر الحجز خادميًا)

### t15: اعتماد الملف النهائي — 10% ~~→ تم~~

الموجود فقط: سجل طلب وقرار، تغيير حالة presentation، وجدول تنزيلات أولي.

التذاكر الحرجة:

- ~~`T15-01`: قفل snapshot حقيقي للإصدار المطلوب ومنع تعديله أثناء المراجعة.~~ — تم (`request_hash` يثبت المحتوى، والقرار يرفض `content_changed`)
- ~~`T15-02`: صلاحية مستقلة لمعتمد الملف ومنع أي مستخدم مصادق من اتخاذ القرار.~~ — تم
- ~~`T15-03`: استدعاء الختم فعليًا وحفظ hash وبيانات الاعتماد مع الملف الرسمي.~~ — تم (`stamp_final_file` يحفظ SHA-256 للملف المادي و`stamped_export_id`)
- ~~`T15-04`: إصلاح مكتبة التنزيلات لتخزن `export_id` ومسارًا مصادقًا صالحًا، لا اسم `download`.~~ — تم (`record_download` يخزن `export_id` و`list_downloads` يعرض رابط `/api/exports/<id>/download` المعتمد)
- ~~`T15-05`: تعبئة حالة الاعتماد والمعتمد والحجم والتوليد والتنزيل تلقائيًا.~~ — تم
- ~~`T15-06`: منع تنزيل النسخة الرسمية قبل الاعتماد، مع السماح بمسودات واضحة.~~ — تم (البوابة تعيد `final_approval_required`، ووضع `?draft=1` يسمي الملف `DRAFT-` مع ترويسة `X-Draft-Mode`)
- ~~`T15-07`: دعم الإعادة بملاحظات وإنشاء إصدار جديد بعد أي تعديل.~~ — تم (إعادة الإرسال دون تغيير ترفض `unchanged_since_rejection`)

### t16: قاعدة عدم التجاوز وصلاحية التعديل بعد التعميد — 15% ~~→ تم~~

الموجود: سبب إلزامي لبعض الانتقالات بعد `approved` وتعريف نظري لحالات القفل.

المفقود أو المعطل:

- ~~توجد انتقالات تسمح بتجاوز البوابات.~~ — تم (`api_update_presentation` يرفض الكتابة المباشرة في الحالات التشغيلية)
- ~~اعتماد التوليد لا يفحص الأقسام ولا الصلاحية.~~ — تم
- ~~اعتماد الملف لا يفحص حالة التوليد أو الإصدار.~~ — تم (`request_final_file_approval` ينقل المسودة عبر `final_approval_pending` قانونيًا ويثبت `request_hash`)
- ~~حفظ وتعديل العرض لا يطلب صلاحية `post-approval edit` مستقلة.~~ — تم (الصلاحية والسبب الكتابي مطلوبان لملف معتمد)
- ~~لا يعاد اعتماد النطاق المتأثر آليًا بعد التعديل.~~ — تم (الحفظ بعد الاعتماد يبطل الاعتمادات المتقادمة عبر مسار الحفظ)

### t18: الأرشفة والاستعادة دون حذف نهائي من واجهة العميل — 35% ~~→ تم~~

الموجود: أرشفة واستعادة وقوائم تعرض المؤرشف.

العطل القاطع:

- ~~زر الحذف النهائي ما زال ظاهرًا ويعمل من شاشة المشروع.~~ — تم (القائمة تعرض أرشفة/استعادة فقط)
- ~~API حذف العرض والمسودة ما زالا متاحين بصلاحيات عميل عادية.~~ — تم (مقيدان بمدير الشركة وبوجود سجل مرافق)
- ~~عبارة الاحتفاظ 365 يومًا مجرد نص.~~ — تم (`archived_at` على المسودات والعروض، و`purge_expired_archives` يحذف ما تجاوز نافذة الاحتفاظ فقط)

### t20: الأدوار التفصيلية — 25% ~~→ تم~~

الموجود: صلاحيات عامة، صلاحيات أقسام، وقوالب سريعة في الواجهة، وCRUD خلفي لقوالب أدوار مخصصة.

المفقود أو المعطل:

- ~~الأدوار التفصيلية التي تعرضها الواجهة مرفوضة عند إنشاء المستخدم.~~ — تم
- ~~لا توجد صلاحيات منفصلة لاعتماد القسم والتوليد والملف والدعم والفوترة والتعديل بعد التعميد والنسخ.~~ — تم (`billing` و`post_approval_edit` و`copy_presentation` مضافة ومفروضة في المسارات)
- ~~قوالب الأدوار الخلفية غير مربوطة بواجهة إدارة فعلية.~~ — تم (إدارة القوالب والإسناد من شاشة المستخدمين)
- ~~قالب «معتمد التوليد» يمنح توليد صور وخرائط ولا يمنح صلاحية اعتماد توليد مستقلة.~~ — تم
- ~~قالب «الدعم» لا يمنح إدارة تذاكر؛ لا توجد هذه الصلاحية أصلًا.~~ — تم
- ~~النطاق على مستوى المشروع غير منفذ.~~ — تم (`user_project_scopes` مع فرض على قراءة الملفات ومسارات التوليد)

### t22: تقرير المستخدمين — 20% ~~→ تم~~

الموجود: API يعيد المستخدمين والدعوات وحساب النشط والمعطل والدعوات المعلقة.

المفقود أو المعطل:

- ~~`record_login` لا يتم استدعاؤها، لذلك `last_login_at` لا يتحدث.~~ — تم (تسجيل عند كل دخول ناجح وبعد تحقق MFA وعند التسجيل بالدعوة)
- ~~الواجهة لا تعرض آخر دخول.~~ — تم
- ~~الخلفية تعيد `pending_invites` بينما الواجهة تقرأ `invited`، لذلك عدد المدعوين يظهر صفرًا.~~ — تم
- ~~الواجهة لا تعرض تفاصيل الدعوات أو المنتهي والمستخدم منها.~~ — تم (قائمة الدعوات مع الحالة والبريد والانتهاء وإعادة الإرسال)
- ~~المستخدمون ذوو الأدوار الجديدة يظهرون غالبًا كـ«موظف» فقط.~~ — تم

### t23: مصفوفة الفصل بين المهام — 30% ~~→ تم~~

الموجود: تقرير يرصد اعتماد الشخص لإصدار قسم أرسله بنفسه، ويرصد بعض القرارات الناقصة السبب.

المفقود أو المعطل:

- ~~لا يفحص الفصل بين معتمد التوليد ومعتمد الملف.~~ — تم
- ~~لا يفحص آخر محرر للملف قبل الاعتماد النهائي.~~ — تم
- ~~لا يفرض أي قاعدة؛ هو تقرير بعد وقوع الحدث فقط.~~ — تم (المصفوفة تقرير، والفصل مفروض في دوال القرار وفي سياسة d01 وصلاحية `post_approval_edit`)
- ~~الواجهة تعلن فصلًا إلزاميًا غير موجود في الصلاحيات أو المسارات.~~ — تم
- ~~لا يغطي شحن الرصيد من مدير النظام ولا صلاحية التعديل بعد التعميد.~~ — تم (شحنات جهة العميل والتعديلات بعد التعميد النهائي بلا صلاحية تظهر في المصفوفة)

### t24: مركز مهام المعتمدين — 85%

الموجود: جدول ودوال للفتح والإغلاق والتذكير والتصعيد.

المفقود أو المعطل:

- ~~لا ينشأ أي `approval_task` تلقائيًا من الاعتمادات الفعلية.~~ — تم (إرسال قسم، طلب توليد، طلب ملف نهائي، طلب شحن، تذكرة دعم — كلها تفتح مهمة وتُغلق عند القرار)
- ~~لا توجد شاشة تعرض `approval_tasks`.~~ — تم (مركز المهام في شاشة العمليات يدمج مهام الاعتماد والمهام اليدوية)
- ~~لا يوجد فرز حسب الشركة والمشروع والقسم والمدة والأولوية.~~ — جزئيًا (العرض مرتب بالاستحقاق ويعرض النوع والمكلف والأولوية؛ فلاتر الشركة/المشروع ما زالت ناقصة)
- لا يوجد تذكير تلقائي أو بريد — التذكير يسجل الوقت فقط.
- لا يوجد scheduler دائم يستدعي التصعيد — يحدث عند استدعاء الكاسحة أو يدويًا.
- ~~لا يوجد تقييد للمهمة على المعتمد المكلف؛ أي مستخدم مصادق داخل الشركة يستطيع التعامل مع المسارات.~~ — تم

### t30: نموذج النقاط الحالي والمحجوز والمتاح والمنتهي — 20% ~~→ تم~~

الموجود: رصيد دولار في الشركة، دفتر فوترة debit/credit، وجدول حجوزات منفصل.

المفقود أو المعطل:

- ~~لا توجد وحدة نقاط موحدة تربط الرصيد والحجز والاستهلاك.~~ — تم (`POINTS_PER_USD` يربط الدولار بالنقاط في `points_overview`)
- ~~الرصيد المتاح لا يطرح الحجوزات المفتوحة.~~ — تم (الحجز يخصم الرصيد كإسكرو و`points_overview` يعرض bucket المحجوز)
- ~~لا يوجد رصيد منتهي أو تاريخ صلاحية للنقاط.~~ — تم (`expires_at` على الحجوزات + bucket `expired` في النظرة العامة)
- ~~لا توجد شاشة تجمع الحالي والمحجوز والمتاح والمنتهي.~~ — تم (بطاقات النقاط الأربع في تبويب المحفظة والشحن تغذيها `GET /api/billing/points-overview`)
- ~~النقاط في جدول الحجز لا تؤثر ماليًا على دفتر الشركة.~~ — تم (حجز/استهلاك/تحرير تقيد حركات hold/debit/release في `tenant_ledger`)

### ~~t31: الحجز الذري والاستهلاك مرة واحدة — 10%~~ — تم

الموجود: رفض تسوية سجل الحجز نفسه مرتين.

العطل القاطع:

- ~~الحجز ليس ذريًا مع الرصيد.~~ — تم (خصم شرطي `UPDATE ... WHERE balance >= cost` داخل معاملة الحجز)
- ~~لا يمنع حجزين من استخدام الرصيد نفسه.~~ — تم (الرصيد ينزل فورًا كإسكرو، والفهرس الفريد يمنع حجزًا نشطًا ثانيًا للعملية)
- ~~لا توجد unique idempotency للعملية أو job في جدول الحجز.~~ — تم (`uq_point_reservations_active_op`)
- ~~الاستهلاك لا ينشئ حركة خصم في الدفتر.~~ — تم (قيد `hold` يعاد تصنيفه إلى `debit` مع استحواذ أحداث الاستخدام)
- ~~الموافقة على طلب التوليد والحجز تحدثان في commits منفصلة.~~ — تم (`decide_generation_approval` يقرر ويحجز في commit واحد)

### ~~t32: الاسترداد والتصحيح والانتهاء والتقرير المفلتر — 20%~~ — تم

الموجود: دفتر debit/credit أساسي، checkout idempotent، وتحرير حجز عند الفشل في بعض المسارات.

المفقود أو المعطل:

- ~~لا توجد أنواع حركة refund وadministrative correction وexpiry في دفتر النقاط.~~ — تم (`LEDGER_ADJUSTMENT_KINDS` + `record_ledger_adjustment`)
- ~~لا توجد حركة عكسية مرتبطة بالحركة الأصلية.~~ — تم (عمود `reversal_of` يتحقق من وجود القيد الأصلي)
- ~~لا توجد صلاحية مدير محددة للاسترداد والتصحيح.~~ — تم (`POST /api/admin/ledger/adjust` خلف `require_admin`)
- ~~API التقرير يقبل `limit` فقط، ولا يفلتر بالفترة والمشروع والملف والمستخدم ونوع الحركة.~~ — تم (`get_ledger_entries` يقبل kind/from/to/draft_id/presentation_id/actor)
- ~~لا يوجد تصدير لتقرير دفتر النقاط.~~ — تم (`export_ledger_csv` عبر `GET /api/admin/reports/ledger`)

### t33: طلب شراء باقة — 30% ~~→ تم~~

الموجود: إنشاء طلب، مراجعة مدير النظام، منع القرار الثاني بعد انتهاء الطلب، رقم مرجعي، وحركة credit idempotent.

المفقود أو المعطل:

- ~~لا توجد واجهة رفع إيصال أصلًا، رغم وجود عمود `receipt_file_id`.~~ — تم (`llUploadRechargeReceipt` يرفع `recharge_receipt` ويرسل `receiptFileId`)
- ~~الباقات والأسعار مكتوبة داخل JavaScript ولا تأتي من `billing_packages`.~~ — تم (`GET /api/billing/packages` يغذي شاشة الشراء)
- ~~المبلغ يقبله الخادم من العميل بدل استخراجه من إصدار باقة موثوق.~~ — تم (`package_id` يحسم السعر خادميًا)
- لا توجد ضريبة أو شروط استخدام أو صلاحية في شاشة الطلب. — الضريبة تمت (أعمدة `tax_rate`/`tax_amount_sar`/`total_sar` على الإيصال)، وباقي شروط الاستخدام وصلاحية الباقة في الشاشة.
- ~~لا يوجد SLA مراجعة 24 ساعة أو إشعار للمدير.~~ — تم (مهمة `due_hours=24` + إشعار + تنبيه `recharge_sla`)
- ~~مدير النظام كان يستطيع إنشاء طلب شحن لحساب المنصة نفسها واعتماده ذاتيًا؛ كانت الواجهة تعرض زر «طلب شحن جديد» والمسار يقبل مستأجر المنصة.~~ — تم (الزر أُخفي عن المدير، والإنشاء والقرار مرفوعان في الخلفية لطلبات مستأجر المنصة)
- ~~اعتماد الطلب وحركة الشحن ليسا معاملة واحدة؛ يتم اعتماد الطلب أولًا ثم محاولة الشحن مع ابتلاع أي استثناء.~~ — تم (الاعتماد يصدر الإيصال بالضريبة في معاملة واحدة)
- ~~لا توجد فاتورة أو إيصال مالي؛ الرقم المرجعي يسمى «سند مالي» في الواجهة فقط.~~ — تم (`topup_receipts` برقم فاتورة تسلسلي `INV-<year>-######` وضريبة وإجمالي وبصمة `receipt_hash`)
- ~~لا يوجد كشف بصمة إيصال أو منع تكرار رقم الحوالة.~~ — تم (`transfer_reference` مفحوص ضد الطلبات السابقة ويرفض التكرار)

### t40: بطاقات الدعم — 35% ~~→ تم~~

الموجود: تذكرة برقم، عنوان، أولوية، حالة، رسائل، أول استجابة، موعد SLA وحالات حل وإغلاق.

المفقود أو المعطل:

- ~~الحالات خمس فقط وليست الحالات السبع المطلوبة، ولا توجد حالة `reopened` صريحة.~~ — تم (سبع حالات تشمل `reopened`؛ رد العميل على `waiting_customer`/`resolved` يعيد الفتح)
- ~~لا توجد قائمة فئات معتمدة أو تحقق منها؛ الواجهة ترسل `general` فقط.~~ — تم (`SUPPORT_CATEGORIES` مع تحقق خلفي وقائمة اختيار في الواجهة)
- ~~لا يوجد مشروع أو ملف مرتبط، ولا مرفقات، ولا مسؤول مكلف.~~ — تم (`project_id`/`draft_id`/`support_ticket_attachments` ومسار إسناد `/assign` بقائمة مستخدمي الشركة)
- ~~SLA ثابت حسب الأولوية فقط ولا يراعي الباقة أو وقت الحل.~~ — تم (`support_sla_policies` بالباقة و`get_sla_for_ticket` يختار هدف الاستجابة والحل)
- ~~لا يوجد تنبيه قبل التجاوز.~~ — جزئيًا (وسم `sla_overdue` وتنبيه اختراق في `platform_alerts`؛ تنبيه استباقي قبل الموعد غير موجود)
- ~~لا توجد إشعارات عند الرد أو تغيير الحالة.~~ — تم (إشعار ومهمة عند الفتح، وإشعار للعميل عند رد المنصة وتغيير الحالة، وللمكتب عند رد العميل)
- ~~أي مستخدم مصادق يستطيع تغيير الحالة؛ لا توجد صلاحية دعم.~~ — تم
- ~~الواجهة لا تعرض تغيير الحالة أو الإسناد أو فلاتر الإدارة.~~ — تم (تغيير الحالة والرد والإسناد وفلتر الإدارة في صندوق دعم المدير)
- ~~قائمة تذاكر مدير النظام كانت تعرض تذاكر مستأجر المنصة فقط.~~ — تم

### t41: إشعارات داخل المنصة والبريد مع سجل تسليم — 15% ~~→ تم~~

الموجود: إنشاء إشعار داخل المنصة، قائمة، وعدّاد قراءة.

المفقود أو المعطل:

- ~~لا يوجد بريد مرتبط بالإشعارات.~~ — تم (`create_notification` يدفع البريد إلى `email_outbox`)
- ~~لا يوجد delivery log أو حالة قناة أو محاولات إعادة.~~ — تم (`notification_deliveries` بالقناة والحالة والمحاولات + `notification_delivery_report`)
- ~~لا تنشأ الإشعارات تلقائيًا من الأحداث الحرجة.~~ — تم (الاعتمادات والشحن والدعم والمهام تنشئ إشعارات)
- ~~لا يوجد سجل «إرسال وقراءة وإجراء»؛ الموجود قراءة فقط.~~ — تم (التسليم مسجل بالقناة والحالة والقراءة على الإشعار)
- ~~إنشاء الإشعار العام لا يتطلب صلاحية إدارية.~~ — تم (الموجه لغير المستخدم أو العام يتطلب `manage_users`/`sag_admin_panel`)

### t42: مهام مرتبطة بالأحداث الحرجة — 10% ~~→ تم~~

الموجود: مهام يدوية أو متكررة يمكن إنشاؤها وإتمامها.

المفقود أو المعطل:

- ~~المهام لا ترتبط تلقائيًا بالأحداث الحرجة.~~ — تم (إرسال قسم/توليد/ملف نهائي/شحن/دعم يفتح `approval_tasks` مرتبطة بالكيان)
- ~~لا يوجد `entity_type/entity_id` في مهام الأحداث اليدوية.~~ — تم
- ~~لا توجد أولوية أو تصعيد أو إشعار أو بريد.~~ — تم (`priority` على المهمة، وتصعيد `approval_tasks` وإشعار المنشئ)
- ~~لا يوجد تقييد حسب المكلف عند القراءة أو الإتمام.~~ — تم

### t52: العقود واتفاقيات السرية — 30% ~~→ تم~~

الموجود: سجل بعنوان ونوع وتاريخ بداية ونهاية وحالة أولية وملاحظات ومعرف ملف اختياري.

المفقود أو المعطل:

- ~~واجهة التسجيل لا ترفع ملفًا ولا ترسل تاريخ بداية أو حالة توقيع.~~ — تم (حقل ملف العقد وتاريخا البداية والانتهاء وحالة التوقيع في النموذج)
- ~~الواجهة ترسل النوع `framework` بينما الخلفية لا تقبله وتحوله بصمت إلى `contract`.~~ — تم (`framework` نوع معتمد في `CONTRACT_KINDS`)
- ~~لا يوجد إصدار للوثيقة أو سجل تعديلات أو حالة توقيع.~~ — تم (`tenant_contract_versions` + `signature_status` أربع حالات)
- ~~لا يوجد تنبيه انتهاء أو ربط تفعيل الشركة باكتمال العقد وNDA.~~ — تم (قائمة اكتمال التفعيل تشترط وثيقة عقد، وانتهاء العقد يظهر في تنبيهات المنصة)
- ~~لا توجد إدارة مستهدفة لعقود شركة معينة من لوحة مدير النظام.~~ — جزئيًا (تاب العقود داخل ملف الشركة يقرأ القائمة؛ التسجيل من ذلك الملف ما زال من شاشة العميل)
- ~~عبارة الاحتفاظ 365 يومًا ليست سياسة منفذة.~~ — تم (`enforce_contract_retention` يوسم `expired` بنافذة `retention_until` ثم `retention_expired` مع قائمة مراجعة التطهير)

### t54: المراقبة التشغيلية دون كشف المحتوى — 20% ~~→ تم~~

الموجود: API يعيد بعض العدادات العامة بلا محتوى عميل.

المفقود أو المعطل:

- ~~الـAPI غير مستدعى من الواجهة.~~ — تم (لوحة المدير تستدعيه)
- ~~لا يعرض زمن التوليد والفشل والأخطاء والتخزين والاستهلاك والتكلفة والتنبيهات.~~ — تم (`storage_overview` و`generation_job_metrics` و`platform_alerts` وعدادات التكلفة)
- ~~لا توجد قياسات أو سجل مهام أو correlation ID.~~ — تم (`generation_jobs` و`job_queue` بمفاتيح correlation/idempotency)
- ~~لا توجد حدود وصول استثنائي للمحتوى؛ توجد مسارات أخرى تسمح لمدير النظام بفتحه مباشرة.~~ — تم (قراءة الملفات عبر الشركات تتطلب منحة `_require_admin_content_access`)

### t55: اللوحات والتقارير القابلة للتصدير — 20% ~~→ تم~~

الموجود: لوحة عميل بسيطة لعدد العروض والاعتمادات الأخيرة، وبطاقات عامة لمدير النظام، وتصدير سجل التدقيق CSV.

المفقود أو المعطل:

- ~~لوحة العميل لا تجمع الدراسات حسب الحالات الإحدى عشرة والمهام والرصيد واستهلاك الشهر والتذاكر وآخر الملفات.~~ — تم (`client_dashboard` + شريط دورة الحياة في الواجهة)
- ~~لوحة سوبر أدمن الشركة لا تعرض الأقسام المتأخرة ومتوسط الاعتماد والاستهلاك حسب المشروع والمستخدم.~~ — تم (`company_admin_dashboard`)
- ~~لوحة مدير النظام لا تعرض التخزين والشحن والتذاكر والإيراد والنقاط وفشل التوليد كما في الوثيقة.~~ — تم (`admin_dashboard` + شريط التنبيهات والمقاييس)
- ~~لا توجد تقارير قابلة للتصدير لدفتر النقاط والاعتمادات والتنزيلات والتذاكر ونشاط المستخدمين والملفات.~~ — تم (`/api/admin/reports/<kind>.csv` بترميز UTF-8 BOM)
- ~~`operational-overview` غير مربوط بواجهة.~~ — تم (لوحة المدير تستدعيه وتعرض العدادات وتوزيع الباقات والإجراءات المعلقة)

### t61: البنية المقترحة — 15% ~~→ تم~~

الموجود: عزل منطقي للشركات في كثير من الاستعلامات، بعض الأعمال الخلفية داخل التطبيق، SMTP أساسي، وتخزين محلي بمجلد لكل شركة.

المفقود أو المعطل:

- ~~لا يوجد تخزين خاص S3 أو KMS أو versioning أو signed URLs أو lifecycle أو فحص برمجيات خبيثة.~~ — تم داخل المستودع: signed URLs (`/api/files/signed/<token>`) و`scan_status` كخطاف فحص وفصل تخزين موثق؛ S3/KMS عناصر نشر في `docs/ARCHITECTURE.md`.
- ~~لا توجد queue دائمة مثل SQS وعمال مستقلون؛ الاعتماد على خيوط داخل عملية Flask لا يضمن الاستمرار.~~ — تم (`job_queue` دائمة مع claim/complete/fail/dead تسمح بعامل مستقل)
- ~~لا يوجد سجل تسليم بريد أو queue أو retries.~~ — تم (`email_outbox` مع محاولات وأخطاء وموعد المحاولة التالية)
- ~~لا توجد WAF أو CloudTrail أو correlation IDs أو لوحات قياس وتنبيهات تشغيلية في هذا المستودع.~~ — تم جزئيًا: correlation IDs على المهام والتوليد ولوحة التنبيهات التشغيلية؛ WAF/CloudTrail خارج نطاق المستودع.
- النشر الحالي الموثق يعتمد cPanel وملفات محلية، وليس البنية المقترحة في الوثيقة. — الهدف موثق في `docs/ARCHITECTURE.md` ويبقى قرار نشر لا كود.

### t62: Registry وVersioned Schema وFlags — 10% ~~→ تم~~

الموجود: سجل بسيط لأنواع المرفقات يحتوي اسمًا ونوعًا وحد حجم وامتدادات ورقم نسخة.

المفقود أو المعطل:

- ~~لا يوجد تعريف لنوع الدراسة وأقسامها وحقولها وقواعد التحقق وتسلسل الاعتماد والتسعير.~~ — تم (`study_types` بإصدارات)
- ~~لا يوجد Versioned Schema لنماذج الإدخال.~~ — تم (`field_schema_versions`)
- ~~لا يوجد registry لمولدات الملفات والمعالج ونموذج المخرجات وقوالب PDF.~~ — تم (`generator_registry`)
- ~~لا توجد إصدارات منفصلة لقالب المحتوى والتصميم والـAI.~~ — تم عبر إصدارات `generator_registry` و`field_schema_versions` و`study_types`
- لا توجد migrations لأنواع الدراسات القديمة. — لا توجد أنواع دراسات قديمة تحتاج ترحيلًا حاليًا؛ آلية الإصدارات جاهزة عند الحاجة.
- ~~لا توجد feature flags للشركات أو rollback.~~ — تم (`feature_flags` + `feature_flag_history` + rollback)
- ~~سجل المرفقات نفسه غير مربوط بالرفع الحقيقي كما ورد في المشكلة الحرجة رقم 15.~~ — تم (`file_type_registry` يحكم قبول الرفع)

### t63: المتطلبات غير الوظيفية وMFA والنسخ والاستعادة وRPO وRTO — 20%

الموجود: JWT، عزل شركة في كثير من الاستعلامات، حماية بعض مسارات الملفات، idempotency في فوترة الاستخدام، واختبارات عزل محددة.

المفقود أو المعطل:

- ~~MFA غير موجود.~~ — تم (TOTP بخطوة تحدي بعد كلمة السر وإعداد إلزامي للمدير)
- ~~لا توجد نسخ احتياطية مشفرة أو point-in-time recovery أو اختبار استعادة دوري.~~ — تم (`scripts/backup_db.py` بنسخة مشفرة + `backup_history` و`scripts/restore_check.py` للتحقق)
- ~~لا توجد قيم RPO وRTO أو خطة طوارئ واختبار سنوي.~~ — تم (RPO/RTO في `docs/ARCHITECTURE.md` وتنبيه خرق RPO في لوحة التشغيل)
- ~~لا يوجد فحص ملفات ضد البرمجيات الخبيثة.~~ — تم (`scan_status` على `project_files` كخطاف فحص؛ الملف المعزول لا يُقدّم)
- ~~لا يوجد correlation ID لكل طلب وتوليد.~~ — تم (مفاتيح correlation/idempotency على `generation_jobs` و`job_queue`)
- الوصولية وRTL/LTR والمنطقة الزمنية ليست مغطاة كعقد غير وظيفي كامل.
- اختبار PostgreSQL والعزل الشامل لكل مسار وملف غير موجود.
- ~~المستخدم المعطل يمكن أن يحتفظ بوصوله عبر JWT سابق حتى انتهاء الرمز.~~ — تم

## مراجعة القرارات العشرة

### ~~d01: اعتماد المحرر لقسمه — 85%~~ — تم

~~تم اختيار «السماح مع التنبيه والتوثيق»، وهو مطبق في اعتماد القسم ويظهر في مصفوفة التدقيق. الناقص أن القرار hardcoded وليس إعداد سياسة واضحًا، ولا توجد اختبارات صلاحية نطاق المشروع والقسم.~~

أصبح القرار سياسة فعلية لكل شركة عبر `section_self_approval` (`allow`/`warn`/`block`) تقرأ من `tenant_policies` وتطبق في `decide_section_version` وتدار من `GET/PUT /api/policies`، مع تغطية اختبارية للأوضاع الثلاثة ولنطاق المشروع والقسم.

### ~~d02: فصل معتمد التوليد ومعتمد الملف — 5%~~ — تم

~~الواجهة تقول إن الفصل إلزامي، لكن لا توجد صلاحيات مستقلة، واعتماد التوليد متاح لأي مستخدم مصادق. القرار المعلن غير مطبق.~~

أصبح القرار مطبقًا فعليًا: صلاحيتان مستقلتان (`approve_generation` و`approve_final_file`)، ومنع الاعتماد الذاتي لغير أدمن الشركة، والواجهة تخفي أزرار القرار عن مقدم الطلب وغير حاملي الصلاحية.

### d03: إعادة توليد PDF دون تغيير — 60% ~~→ تم~~

~~تصدير PDF من الشرائح الحالية لا يستدعي AI بطبيعته، بينما إعادة توليد الشرائح تمر ببوابة التوليد. الناقص سياسة موثقة مرتبطة بـhash الإصدار وسجل تكلفة واضح.~~

أصبح كل تصدير يحمل `content_hash` و`regen_policy` (`official`/`superseded`/`draft`) و`cost_usd` من `_export_regen_policy`، فالملف يعرف إن كان يطابق ما اعتمده المعتمد أم تجاوزه، وتكلفة كل إصدار مسجلة معه.

### d04: الإلغاء والاسترداد عند الفشل الجزئي — 20% ~~→ تم~~

~~يوجد تحرير حجز من JavaScript عند بعض حالات فشل الشرائح، لكن لا توجد سياسة كاملة ولا معالجة خادمية لانقطاع العميل أو فشل الخطة أو الفشل الجزئي، والحجز نفسه غير مالي.~~

الحجز مالي إسكرو الآن: الفشل الجزئي يسوّي الفرق (`settle` بخصم ما نجح فقط وإرجاع الباقي)، وفشل الخطة أو العرض يحرر الحجز كاملًا، وانقطاع العميل تعالجه كاسحة `release_stale_reservations` و`sweep_stale_generation_jobs` خادميًا — الاسترداد يعود حركة `release`/`refund` في الدفتر.

### d05: مدة اعتماد القسم قبل اعتباره قديمًا — 0% ~~→ تم~~

~~لا توجد مدة أو إعداد. الموجود فقط إبطال الاعتماد عند تغير hash المحتوى.~~

أصبح للاعتماد نافذة زمنية: `tenant_branding.section_approval_days` يضبط المدة (افتراضي 30 يومًا)، و`decide_section_version` يختم `expires_at`، و`is_expired`/`expired_approved_sections` يكشفان الاعتمادات المنتهية، و`create_generation_approval` يرفضها بـ`section_version_expired`.

### d06: مدة الاحتفاظ بعد انتهاء العقد — 10% ~~→ تم~~

~~تظهر عبارة 365 يومًا في الواجهة، لكن لا توجد سياسة قاعدة بيانات أو scheduler أو حذف إداري أو ربط بانتهاء العقد. هو نص فقط.~~

`enforce_contract_retention` سياسة منفذة: العقد المنتهي يوسم `expired` ويُختم `retention_until = الانتهاء + CONTRACT_RETENTION_DAYS`، وما تجاوز النافذة يقلب `retention_expired` ويُدرج مالكه في `tenants_pending_review` لمراجعة التطهير — دون حذف صامت. يُستدعى من مسار المدير ومن `platform_alerts`.

### ~~d07: فتح مدير النظام لمحتوى العميل — 0%~~ — تم

~~الوصول الكامل متاح مباشرة لمدير النظام دون سبب أو موافقة أو إشعار. الوضع الحالي عكس المطلوب.~~

تطبق الآن دورة كاملة: طلب وصول بسبب إلزامي ونطاق (شركة/عرض/ملف) ومدة، ثم قرار مدير الشركة، ثم منحة محدودة زمنيًا تُحصى استخداماتها وتُدقق داخل شركة العميل مع إشعار عند أول وصول وإمكانية الإلغاء قبل الانتهاء. بدون منحة تعيد مسارات المحتوى `access_grant_required` مع 403.

### d08: زمن الاستجابة لكل أولوية وباقة — 25% ~~→ تم~~

~~توجد ساعات ثابتة حسب أولوية التذكرة فقط. لا توجد فروق حسب الباقة أو هدف حل أو تنبيه قبل التجاوز أو إعداد سياسة.~~

`support_sla_policies` تحمل هدفي أول استجابة وحل لكل (أولوية × باقة)، و`get_sla_for_ticket` يختار سياسة باقة الشركة ثم الافتراضي، وشاشة إعدادات المنصة تدير الأهداف، والتجاوز يوسم التذكرة `sla_overdue` ويظهر تنبيهًا في لوحة التشغيل.

### d09: الباقات والضريبة والمستند المالي — 15% ~~→ تم~~

~~توجد أسعار ثابتة ومرجع شحن، لكن لا توجد ضريبة أو إصدار فاتورة أو مستند مالي حقيقي، ولا ربط بإصدار باقة.~~

طلب الشحن يرتبط بإصدار باقة (`package_version_id`) والسعر يُحسم خادميًا، واعتماده يصدر `topup_receipts` برقم فاتورة تسلسلي وضريبة (`tax_rate`/`tax_amount_sar`/`total_sar`) ومرجع الحوالة وبصمة `receipt_hash` في المعاملة نفسها.

### d10: حدود الملفات لكل شركة وباقة — 10% ~~→ تم~~

~~يوجد سجل حدود عالمي بسيط، لكنه غير مطبق على الخادم وغير مربوط بالشركة أو الباقة، والتحقق الأمامي غير مستخدم ومعطل نوعيًا.~~

`_store_project_upload` يفرض امتدادات وحد حجم القاعدة الفعالة في `file_type_registry` (بأسماء بديلة لأنواع الرفع)، ويقرأ `max_file_mb` من إصدار باقة الشركة النشطة عبر `current_package_limits`، فيصبح الحد لكل شركة حسب باقتها لا قيمة عالمية.

## مخالفات إضافية لتعليمات AGENTS.md

هذه ليست ضمن البنود الإحدى والثلاثين فقط، لكنها موجودة في الحالة الحالية:

- ~~توجد نصوص تعليمية مباشرة على الشاشة رغم قاعدة «لا نصوص how-to».~~ — تم (أزيلت العبارات التعليمية ويحرسها `test_ui_carries_no_static_how_to_hints`)
- ~~زر «حذف المشروع» يخالف قاعدة الأرشفة المطلوبة في نفس ملف المهام.~~ — تم (أرشفة/استعادة بدل الحذف)
- `task.html` ملف محلي فقط حسب التعليمات ويجب ألا يدخل أي commit أو push.
- ~~سجل أنواع الملفات يعطي انطباعًا بوجود سياسة رفع مركزية بينما الخادم لا يستخدمه.~~ — تم (السجل يحكم الرفع فعليًا)

## نتائج الاختبارات والفحوصات

نجحت الفحوصات التالية:

- `tests.test_section_versions`: عدد 21 اختبارًا.
- `tests.test_audit_log`: عدد 10 اختبارات.
- `tests.test_proposal_lifecycle`: عدد 10 اختبارات.
- `tests.test_landloom_platform`: عدد 55 اختبارًا.
- `tests.test_admin_company_accounts`: عدد 13 اختبارًا.
- `tests.test_packages_fx`: عدد 8 اختبارات.
- `tests.test_client_overview`: عدد 4 اختبارات.
- `tests.test_billing_ledger`: عدد 12 اختبارًا.
- `tests.test_meeting_requirements`: عدد 316 اختبارًا.
- `tests.test_export_slide_sanitization`: عدد 42 اختبارًا.
- `tests.test_location_data_parsing`: عدد 10 اختبارات.
- `tests.test_project_draft_isolation`: عدد 5 اختبارات.
- `tests.test_font_workflows`: عدد 23 اختبارًا.
- `tests.test_admin_agent`: عدد 13 اختبارًا.
- فحص الواجهة `scripts/verify-frontend.js` نجح لكل ملفات JavaScript والـbundles.
- فحص Python syntax نجح.

إجمالي اختبارات unittest التي شغلت في المراجعة: **أكثر من 540 اختبارًا ناجحًا**.

نجاح الاختبارات لا يعني اكتمال المهام للأسباب التالية:

- معظم اختبارات `test_landloom_platform.py` تختبر دوال قاعدة البيانات مباشرة، ولا تختبر دورة العمل المترابطة أو البريد أو الواجهة.
- ~~اختبار اعتماد التوليد يسمح صراحة بأن يكون مقدم الطلب والمعتمد هو الشخص نفسه.~~ — تم (الاختبارات تستخدم معتمدًا منفصلًا وتغطي رفض الاعتماد الذاتي)
- ~~اختبار اعتماد الملف يبدأ بإدخال عرض حالته `approved` ثم يطلب اعتماده النهائي، لذلك لا يثبت دورة الحالات الصحيحة.~~ — تم (اختبارات `test_landloom_platform` و`test_workflow_gates` تمشي بالدورة القانونية عبر `final_approval_pending`)
- ~~لا يوجد اختبار يستدعي `stamp_final_file`.~~ — تم (الختم يعمل داخل `decide_final_file_approval` التي تغطيها الاختبارات)
- ~~لا توجد اختبارات تكامل لربط `approval_tasks` أو الإشعارات بالأحداث.~~ — تم (فتح المهمة والإشعار مغطيان عند إرسال القسم وطلب التوليد والملف والشحن والدعم)
- الاختبارات تعمل على SQLite ولا تثبت immutability وسلامة المعاملات على PostgreSQL.

## ملاحظات تشغيلية ظهرت أثناء الفحص

- ظهرت استجابات فعلية من Google Maps تفيد بأن مفتاح API المحمل في بيئة الفحص منتهي الصلاحية: `API key expired` و`REQUEST_DENIED`. يلزم التحقق من مفتاح بيئة التشغيل قبل الاعتماد على الخرائط.
- ظهرت تحذيرات استخدام `datetime.utcnow()` المتقادم في Python 3.14، وهذا يحتاج تنظيفًا قبل أن يتحول إلى كسر في إصدار لاحق.
- ظهرت `ResourceWarning` لملفات غير مغلقة في بعض الاختبارات ومسارات التصدير.

## ترتيب الإصلاح المقترح

هذا ترتيب مخاطر، وليس كودًا أو خطة زمنية:

1. ~~منع تجاوز الصلاحيات في اعتماد التوليد والملف وتغيير حالات الدعم والمهام.~~ — تم
2. ~~توحيد دورة الحالات وربط البوابات الثلاث فعليًا.~~ — تم
3. ~~إعادة تصميم الحجز والخصم كتسوية ذرية واحدة على دفتر النقاط الحقيقي.~~ — تم
4. ~~إصلاح اعتماد الملف والختم ومكتبة التنزيلات.~~ — تم
5. ~~إصلاح الأدوار التفصيلية وتعطيل المستخدم وآخر سوبر أدمن وMFA.~~ — تم
6. ~~ربط المهام والإشعارات والبريد بكل الأحداث الحرجة.~~ — تم
7. ~~إزالة الحذف النهائي من واجهة العميل.~~ — تم
8. ~~منع وصول مدير النظام إلى المحتوى إلا بمسار استثنائي مسبب ومسجل ومُشعر.~~ — تم
9. ~~ربط الشحن بالباقات الحقيقية والإيصال والضريبة والمعاملة الذرية.~~ — تم
10. ~~استكمال الدعم والعقود والتقارير والبنية والنسخ والاستعادة والمرونة.~~ — تم
