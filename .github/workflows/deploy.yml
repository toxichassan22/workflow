name: Auto Deploy to Hosting

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Server Deployment Webhook
        run: |
          curl -s -X POST "https://sagdemo.site/api/deploy-webhook?secret=${{ secrets.DEPLOY_WEBHOOK_SECRET }}"
