# مشاكل ومخاطر النظام

تاريخ المراجعة: 2026-09-15.

النسخة المراجعة: الفرع `lab`، commit `d57875ca06824f90923b49c22671da04bd034380`، مع قراءة الملفات الموجودة محليًا وقت المراجعة.

## نطاق التقرير وطريقة قراءة النتائج

مراجعة واسعة لمسارات Flask، المصادقة والصلاحيات، قاعدة البيانات، المسودات والإصدارات والاعتمادات، الفوترة والمفاتيح، التوليد والتحليل والخرائط، الواجهة والدراسة المالية، التصدير، المهام والإشعارات، Docker والنشر والنسخ الاحتياطي والاختبارات. التركيز على المسارات الفعلية والمكونات المرتبطة بها، وليس اعتبار كل ملف قديم في المستودع جزءًا نشطًا من التطبيق.

هذا تقرير بالمشكلات التي أمكن تحديدها، وليس ضمانًا باكتشاف كل عيب ممكن. لم تُستخدم بيانات العملاء، ولم يُفتح محتوى ملفات الأسرار، ولم تُجرَ اختبارات اختراق على خدمة حية أو طلبات توليد مدفوعة. لم تُستورد `app.py` للتشغيل، لأن استيرادها ينفذ تهيئة قاعدة البيانات. لم تُشغّل عمليات نشر أو إرسال بريد أو نسخ احتياطي. الملف الوحيد الذي أُنشئ هو هذا التقرير؛ لم تُعدّل الأكواد ولم يُنفذ commit أو push.

المواضع أدناه بصيغة `الملف:الأسطر`، نسبة إلى جذر المشروع، وقد تتغير بعد تعديل الكود.

### درجات الخطورة والثقة

- **P1 — عالية:** خلل صلاحيات أو خصوصية أو أموال أو فقد بيانات أو توقف مسار أساسي.
- **P2 — متوسطة:** نتيجة خاطئة أو تعطل في ظرف محدد أو مشكلة تشغيل واعتمادية.
- **مُعاد إنتاجها:** شُغلت الدوال أو المقاطع الفعلية المعنية، مستخرجة من المصدر، داخل SQLite في الذاكرة أو بيئة بديلة محدودة، دون تشغيل النظام الحقيقي.
- **مؤكدة من الكود:** المسار والسبب ظاهران في المصدر؛ لا تعني أن حادثة حدثت على الإنتاج.
- **مخاطرة مشروطة:** يتوقف حدوث الضرر على إعداد أو توقيت أو استعمال موضح في البند.

### التحقق المنفذ

- نجح تحليل AST لعدد **80 ملف Python متتبعًا**، دون استيراد التطبيق أو إنشاء bytecode.
- نجح فحص صياغة **21 ملف JavaScript للواجهة** وتجميع أجزاء التطبيق داخل الذاكرة.
- نجح `node tests/test_presentation_save.js`.
- نجح `node tests/test_presentation_undo.js`.
- أُعيد إنتاج **14 حالة** باختبارات محدودة في الذاكرة، موضحة في البنود المعنية. البيئات البديلة تعزل موضع العيب؛ لا تحل محل اختبار تكامل كامل.
- لم تُشغّل مجموعة Python الكاملة، ولم يُبن Docker، ولم يُستخدم PostgreSQL أو SMTP أو مزود AI فعلي. النتائج الخاصة بتلك البيئات تحليل مصدر، وليست ادعاء نجاح أو فشل اختبار حي.
- حالة Git قبل المراجعة كانت تحتوي تعديلًا سابقًا في `task.html` وملفات محلية غير متتبعة؛ لم يُمس أي منها.

## أولًا: الهوية والصلاحيات والخصوصية

### ~~ISS-001 — حماية السوبر أدمن تقبل حساب مستخدم معطل أو محذوف~~

**تم الإصلاح في commit `60b4f5d` ثم `d6d6e7d`.** صلاحية المنصة لم تعد تُستمد من `is_admin` على الشركة لأي token مرتبط بمستخدم؛ `_is_platform_admin_session` يسمح فقط بجلسة tenant-direct الخاصة بحساب المنصة نفسه، و`require_admin` يرفض أي token يحمل `user_id` حتى لو كان دوره `company_admin`. التحقق الحي عبر `_load_token_user` يرفض المستخدم المعطل أو المحذوف أو المنتقل لشركة أخرى، و`/api/auth/me` ومسارات إصدار الجلسة لم تعد تعلن `isAdmin` لجلسة مستخدم. مغطاة باختبار `test_platform_admin_is_only_the_tenant_account` في `tests/test_identity_security.py`.

### ~~ISS-002 — حساب مدير الشركة له هويتان منفصلتان للمصادقة~~

**تم الإصلاح في commit `3e78ddf`.** سجل `tenants` هو هوية المالك الواحدة للمصادقة، وسجل `users` للمدير الأساسي أصبح سجل إدارة وبوابة حالة فقط. تسجيل الدخول يحل المستخدم الأساسي عبر `primary_user_id` ويتحقق من كلمة المرور على سجل المستخدم (المصدُر) ويعتمد هاشه على سجل الشركة عند الانحراف، فلا تبقى كلمة مرور قديمة على `tenants` صالحة للالتفاف. التوكن المرتبط بالمستخدم الأساسي يُطبَّع لجلسة tenant-direct بإصدار الجلسة ومفتاح الملكية `tenant-admin:<id>`، وتعطيل المستخدم أو تنزيله أو حذفه يغلق مسار المالك. `update_user` يعكس تعديلات الأساسي على سجل الشركة ويسقط جلستي الهويتين عند تغيير كلمة المرور، ومايجراشن `_migrate_primary_admin_identity` يزيل التكرار بين سجل المستخدم وسجل الشركة. مغطاة بفئة `PrimaryAdminIdentityTests` (14 اختبارًا) في `tests/test_identity_security.py`.

### ~~ISS-005 — لا توجد حدود محاولات داخل التطبيق لتسجيل الدخول~~

**P1 | مخاطرة مشروطة بغياب حماية خارجية مكافئة.**

**الموضع:** `app.py:14987-15140,15257-15327`؛ `auth.py:188-208,259-262`.

لم يظهر limiter أو عداد محاولات أو قفل مؤقت على تسجيل الدخول أو التسجيل العام. هذا يعرض كلمات المرور للتخمين ويستهلك المعالجة. وجود WAF مناسب على الاستضافة لم يُتحقق منه؛ لا يوفره الكود نفسه.

### ~~ISS-006 — تغيير كلمة المرور والخروج لا يبطلان الجلسات السابقة~~

**تم الإصلاح.** كل توكن يحمل الآن `jti` فريدًا و`sv` (إصدار الجلسة)، والتحقق يرفض التوكنات المدرجة في `revoked_tokens` أو التي لا يطابق `sv` الخاص بها صف الحساب الحالي. مسار `POST /api/auth/logout` يبطل التوكن المعروض على الخادم قبل أن يحذفه العميل محليًا، وكل تغيير لكلمة المرور — تعديل مستخدم أو شركة، مزامنة المشرف الأساسي، إكمال رابط الإعداد، أو إعادة تعيين من المشرف العام — يرفع `session_version` فيُسقط كل الجلسات السابقة، و`/api/auth/refresh` يرفض التوكنات المبطلة أو القديمة. مغطى في `tests/test_identity_security.py`.

### ~~ISS-007 — رابط إعداد كلمة المرور قد يعيد تفعيل مستخدم~~

**تم الإصلاح.** أصبح استهلاك الرابط ذريًا (`UPDATE` مشروط بـ`used_at IS NULL` مع فحص `rowcount`، فطلب ثانٍ متزامن يفشل)، وتعطيل المستخدم أو الشركة يلغي روابط الإعداد غير المستخدمة عبر `_revoke_password_setup_tokens` في `update_user` و`update_tenant` و`sync_primary_company_admin` و`set_tenant_active`. مغطى في `tests/test_admin_company_accounts.py`.

### ~~ISS-008 — سياسات كلمات المرور متناقضة، والوكيل يملك كلمة افتراضية ثابتة~~

**تم الإصلاح.** كل مسار يقبل كلمة مرور (تسجيل شركة، إضافة موظف، تعديل مستخدم، قبول دعوة، إنشاء/إعادة تعيين من المشرف العام، `add_user` في الوكيل) يمر الآن على `_password_validation_error` الموحّد — عشرة أحرف على الأقل مع حروف وأرقام — والواجهة تفرض نفس القاعدة عبر `passwordPolicyError` قبل إرسال الطلب. الوكيل لم يعد يملك كلمة افتراضية: كلمة مُرسلة تُتحقق، وغيابها ينشئ الحساب خلف رابط تعيين لمرة واحدة مع `require_password_change` دون أن تعود أي كلمة مرور في النتيجة. مغطى في `tests/test_identity_security.py` و`tests/test_admin_agent.py`.

### ~~ISS-009 — صلاحية بيانات التدريب تمنح الوكيل قدرات إدارة مستخدمين وإعدادات لا يملكها صاحب الطلب~~

**أُصلحت.** `AGENT_TOOL_PERMISSIONS` يربط كل أداة مؤثرة بالصلاحية التي يتطلبها مسارها المخصص (`manage_users` لأدوات المستخدمين، `company_settings` للهوية والفريق والخط وقواعد التوليد، `custom_fields` للحقول والأقسام، `create_presentation` لمساحة العمل، `export_files` للتصدير، `training_data` لأدوات التدريب نفسها)، و`_execute_agent_action` يعيد فحص صلاحية صاحب الطلب عبر `_agent_requester_permission_granted()` — بنفس منطق `require_permission()`: المشرف العام ومدير الشركة والدخول المباشر يمرون، وغيرهم يحتاج الصلاحية ممنوحة على حسابه. الأداة المرفوضة ترجع `error_code: AGENT_PERMISSION_DENIED` دون تغيير أي حالة، والأدوات القرائية المتاحة لأي مستخدم تبقى بلا ربط. مغطاة باختبار `test_agent_tools_respect_the_requesters_own_permissions` في `tests/test_admin_agent.py`.

### ~~ISS-010 — أداة تعديل الصلاحية تنفذ التغيير ثم تبلغ المستخدم بفشل~~

~~**P2 | مُعاد إنتاجها.**~~

~~**الموضع:** `app.py:23259-23293,23792-23795`.~~

~~فرع `set_permission` يستعمل `status_text` في رسالة النتيجة قبل تعريفه. التعريف موجود في فرع آخر هو `toggle_user`، فلا يعمل في هذا الاستدعاء. يحصل الخطأ بعد `db.set_user_permission` وتسجيل التغيير. الاختبار أكد حدوث التعديل في البديل المعزول ثم رجوع `status='error'` بسبب `UnboundLocalError`؛ المستخدم قد يعيد المحاولة أو يعتقد أن الصلاحية لم تتغير.~~

**أُصلحت:** عُرّف `status_text` داخل فرع `set_permission` («منح»/«سحب») وطُبّعت قيمة `granted` كما في `toggle_user` حتى لا تُحسب السلسلة `'false'` منحة. يغطيه `test_set_permission_reports_the_change_it_applied` في `tests/test_admin_agent.py`.

### ~~ISS-011 — حماية آخر مدير شركة يمكن تجاوزها عبر أداة الوكيل~~

**أُصلحت.** أداة `toggle_user` في الوكيل تفحص `db.is_primary_company_admin` قبل التعطيل وترفض برسالة «لا يمكن تعطيل مدير الشركة الأساسي»، بنفس حارس مساري `api_update_user` و`api_delete_user`. في النموذج الحالي مدير الشركة هو هوية المستأجر الأساسية (`is_primary_company_admin`) لا دورًا قابلًا للتعدد، والاختبارات في `tests/test_identity_security.py` حُدّثت على هذا المعنى.

### ~~ISS-012 — حالة اعتماد عرض يمكن قراءتها من شركة أخرى~~ — تم الإصلاح

**أُغلقت في commit `9e1118c`:** المسار أصبح يتحقق أن العرض مملوك للشركة (`get_presentation` بـ`tenant_id`) ويطبق نفس فحص نطاق المشروعات الخاص بمسار GET، و`db.get_approval_status` تقبل `tenant_id`. مغطاة باختبار `test_presentation_approval_status_is_tenant_and_scope_guarded` في `tests/test_identity_security.py`.

### ~~ISS-013 — مفتاح منع تكرار مهمة التوليد غير معزول بين الشركات~~

~~**P1 | مُعاد إنتاجها.**~~

~~**الموضع:** `db.py:13067-13099,8399`؛ `app.py:24845-24863`.~~

~~`create_generation_job` يبحث عن `idempotency_key` عالميًا ويرجع السجل كاملًا دون مطابقة الشركة أو الاعتماد. الفهرس نفسه عالمي. إنشاء مهمة باسم مفتاح موجود لدى شركة أخرى أعاد في الاختبار مهمة الشركة الأولى، بما فيها الشركة والاعتماد. المسار المستدعي يستطيع أيضًا ربط اعتماد الشركة الحالية بمعرف المهمة المسترجع خطأ.~~

**أُصلحت:** أصبح نطاق المفتاح `(tenant_id, approval_id, idempotency_key)` في البحث وفي الفهرس الفريد الجديد `ux_generation_jobs_tenant_idem` (مع إسقاط `ux_generation_jobs_idem` العالمي عند التهيئة). إعادة الإرسال داخل نفس الاعتماد ترجع نفس المهمة، بينما المفتاح نفسه لدى شركة أخرى أو اعتماد آخر ينشئ مهمة مستقلة، وسباق الإدراج المتزامن يعيد صف الفائز بدل الخطأ. وربط `generation_approvals.job_id` في المسار أصبح مشروطًا بانتماء المهمة لنفس الاعتماد والشركة. يغطيه `test_job_idempotency_key_stays_inside_its_tenant_and_approval` في `tests/test_workflow_gates.py`.

### ~~ISS-014 — نطاق المشروعات المسموح بها لا يطبق على مسارات موازية~~

**أُصلحت.** `_presentation_in_scope(pres)` أصبح شرط كل مسارات العرض المرتبطة بمسودة (GET/PUT/الإصدارات/المقارنة/الاستعادة/سجل التعديلات/طلب الاعتماد وحالته/الأرشفة والاسترجاع/التصدير/تسجيل التنزيل)، و`_generation_job_in_scope` و`_project_file_in_scope` تغطيان مهام التوليد وملفات المشروع وروابطها الموقعة ونشر الصور. قوائم `list_generation_jobs` و`list_final_file_approvals` و`list_downloads` و`get_pending_project_drafts` و`get_pending_approvals` و`get_presentations` تقبل `accessible_draft_ids`، والمسارات التي تستقبل `draftId` (حفظ مسودة، طلب اعتماد، مراجعة، نسخ، حالة قسم، دورة حياة) تفحص `db.user_may_access_draft` قبل الفعل. المورد خارج النطاق يجاوب not-found ولا يكشف وجوده، وجلسة مدير الشركة تبقى بلا قيد. مغطاة في `tests/test_issues_014_020_security.py`.

### ~~ISS-015 — إخفاء الأقسام ليس منعًا لقراءة بياناتها، وبعض الأقسام خارج نموذج الصلاحيات~~

**أُصلحت.** أقسام الويدجت (`section-timeline`، `section-financial-calc`، `section-team`، `section-market-study`، `section-visual-concept`، `section-executive-content`) انضمت إلى `db.FIELD_SECTIONS` فأصبحت قابلة للمنح والسحب مثل أي قسم حقول، وكتلها في `DRAFT_FIELD_SECTION_BLOBS` و`SECTION_SNAPSHOT_BLOBS`. ردود المسودة و`projectData` في العرض ونسخ الإصدارات والمقارنة تمر على `_draft_data_for_response`/`_draft_response_filtered` فلا يصل قسم محجوب للعميل. الكتابة تمر على `_enforce_draft_field_sections` في حفظ المسودة وحفظ العرض معًا: تغيير محتوى محجوب يرفض بـ`SECTION_FORBIDDEN`، وإغفال مفتاحه يستعيد القيمة المخزنة بدل مسحها، وحالات الأقسام المحجوبة تُستعاد أيضًا. إرسال/قرار/إلغاء/استرجاع إصدار قسم وقراءة لقطته وتبديل حالته ترفض القسم المخفي، والواجهة لا تضيف أقسام الويدجت أصلًا عند سحب الصلاحية. مغطاة في `tests/test_issues_014_020_security.py`.

### ~~ISS-016 — تجميع أجزاء الطلب يحدث قبل المصادقة وبمجلد غير مربوط بالشركة~~

**أُصلحت.** مجلد الأجزاء أصبح `.body_chunks/<tenant>/<scope>/<upload_id>` حيث `scope` هو `u<user_id>` أو `tenant` للجلسة المباشرة (مع تطبيع هوية مدير الشركة الأساسي لنطاق المستأجر كما في ISS-002). `_body_chunk_namespace()` يستخرج الهوية من نفس JWT — داخل hook التجميع قبل المسار، وداخل مسار الرفع من `g` — فالمرجع لا يحل إلا داخل نطاق صاحبه، وطلب شركة أخرى أو بلا جلسة يجاوب «مرجع غير صالح» ولا يستهلك أجزاء أحد. المسار الاحتياطي داخل `/api/export` يستخدم نفس الدليل، وكنس المجلدات القديمة يمشي المستويين الجديدين. مغطاة في `tests/test_issues_014_020_security.py`.

### ISS-017 — فك الضغط يسمح بحجم فعلي أكبر كثيرًا من حد الطلب

**P1 | مؤكدة من الكود. — مؤجلة بقرار المالك (لا مشكلة في حجم الملفات حاليًا).**

**الموضع:** `app.py:58,97-113,176-185`.

الحد 50MB يطبق على جسم النقل، بينما `gzip.decompress` يفك المحتوى دفعة واحدة دون سقف للحجم الناتج أو نسبة الضغط. الأمر يحدث أيضًا في hook عام قبل المصادقة. طلب صغير عالي الانضغاط يستطيع استهلاك ذاكرة كبيرة، وجمع الأجزاء لا يفرض حدًا إجماليًا على المحتوى بعد فك الضغط. لم يُرسل أي payload ضغط مؤذٍ خلال المراجعة.

### ISS-018 — HTML غير موثوق يصل إلى DOM التطبيق دون عزل أو تنقية أمنية

**P1 | مؤكدة من الكود؛ لم ينفذ JavaScript هجومي. — مؤجلة بقرار المالك (إدارة الشركة مسؤولة عن منع الوصول، وقد تُحل تطوعيًا لاحقًا).**

**الموضع:** `assets/js/15-slide-edit-chat.js:217-227`؛ `assets/js/13-visual.js:1156-1158,1274-1287,1328-1329`؛ `assets/js/09-financial.js:711-714,740-742`؛ `assets/js/00-core.js:2-6`.

الشرائح تُحقن في `innerHTML` داخل نفس صفحة الحساب، و`processSlideHtmlClient` يعالج الخطوط والـplaceholders لا event handlers أو عناصر HTML النشطة. كذلك أسماء وصيغ صفوف مالية تدخل attributes دون escaping، وtoast يعرض الرسالة كـHTML. وجود بيانات محفوظة أو مولدة غير آمنة قد يؤدي إلى stored XSS. الخطر أكبر لأن توكن الجلسة موجود في localStorage (`assets/js/01-nav-auth.js:268-270`).

### ~~ISS-019 — التصدير يستطيع تحميل عناوين داخلية وقراءة صور محلية خارج نطاق الشركة~~

**أُصلحت.** سياسة موارد موحدة في `generate_pdf_from_preview`: `_export_allowed_local_path` يسمح فقط بملفات تحت `uploads/<tenant>` و`uploads/creative/<tenant>` و`uploads/training/<tenant>` و`uploads/maps/` المملوكة عبر سجل `map_images` و`assets/` و`fonts/` ومجلد التصدير المؤقت، و`_export_url_allowed` يقبل `data:image`/`data:font` وروابط الخطوط (`fonts.googleapis.com`/`fonts.gstatic.com`) فقط — كل HTTP/HTTPS آخر و`file://` خارج الجذور و`javascript:` و`..` مرفوضة. `_sanitize_export_resource_urls` يفرغ المراجع المرفوضة من `src/href/srcset/poster` و`url()` و`@import` بعد حلول التحويل الشرعي، و`_install_export_request_guard` يقطع أي طلب شبكي خارج القائمة في صفحات Chromium الثلاث (الفردي والمقسّم ولقطة الشريحة). `_resolve_image_bytes` في PPTX أُعيد كتابته على نفس السياسة: لا جلب بعيد إطلاقًا، والمسارات المحلية و`tenant-assets/` والاختصارات لا تُقرأ إلا داخل جذور الشركة. مغطاة في `tests/test_issues_014_020_security.py`.

### ~~ISS-020 — مرجع صورة التوليد يتحقق من uploads فقط وليس من مالك الصورة~~

**أُصلحت.** `_uploads_reference_owned_by_tenant` يقبل مرجع `uploads/` فقط داخل مساحة الشركة المستدعية (`uploads/<tenant>`، `uploads/creative/<tenant>`، `uploads/training/<tenant>`)، وملفات `uploads/maps/` المشتركة تُثبت ملكيتها بسطر في سجل `map_images` للشركة. الروابط البعيدة مرفوضة كليًا و`data:` يمر كما هو لأنه بايتات العميل نفسه. معرفات ملفات المشروع تُحل عبر `db.get_project_file(tenant_id, ...)` فلا يملك مرجع شركة أخرى، والمستأجر يُمرر صراحة عبر `usage_ctx` في كل مسارات التوليد والتصور البصري والمصمم لأن الاستدعاءات تعمل في خيوط بلا سياق طلب. مغطاة في `tests/test_issues_014_020_security.py`.

### ~~ISS-021 — طبقة حفظ الإصدارات تتجاهل أخطاء تجميد الأصول في مسارات حساسة~~

**أُصلحت.** الاستثناء أصبح لكل مرجع على حدة عبر `preserve_missing_uploads` داخل الـfreezer نفسه: يُحفظ فقط مرجع legacy معلّق يبدأ بـ`/uploads/maps/` أو `/uploads/creative/` **وملفه غير موجود فعلًا** (محتوى منقول قديم)، بينما أي مرجع آخر فاشل — ملف موجود لشركة أخرى، مسار غير آمن، symlink، نوع مرفوض — يُرفض. الـwrapper في `app.py` يمرر العلم ولم يعد يبتلع الخطأ، و`PresentationAssetError` يترجم لرد HTTP واضح عبر errorhandler بدل 500. مغطاة في `tests/test_presentation_assets.py`.

### ~~ISS-022 — الصور المنشورة ومراجعاتها متاحة بروابط عامة دائمة~~

**أُصلحت.** مسارات `/uploads/maps|creative/...` و`/tenant-assets/...` لم تعد تخدم ملفات الوسائط بلا إثبات: يُقبل إما توقيع `itsdangerous` منتهي الصلاحية مربوط بالمسار والشركة، أو جلسة JWT حية للشركة المالكة (بنفس فحوص `require_auth` — صف نشط، مستخدم حي، جلسة غير ملغاة). ردود JSON تُوقّع روابط `/uploads/` المملوكة للشركة تلقائيًا عبر `after_request` قبل الضغط، والتصدير لا يتأثر لأنه يحل الأصول محليًا عبر `file://`. `tenant-assets` (شعار/خطوط) تبقى شبه عامة بطبيعتها خارج النطاق. مغطاة في `tests/test_issues_014_020_security.py`.

## ثانيًا: الاعتمادات وسلامة المسودات والإصدارات

### ~~ISS-023 — الحفظ العادي يستطيع كتابة اعتمادات الأقسام خارج مسار الاعتماد~~

**أُصلحت.** حفظ المسودة أصبح يمرر `sectionStatuses` المرسلة على نفس بوابات المسار المخصص: الترقية إلى `approved` تتطلب شروط الجاهزية نفسها (اعتماد خرائط الموقع، snapshots معتمدة وغير منتهية)، والمفاتيح المحذوفة من الخريطة تُستعاد من التخزين، والتخفيض يبقى مسموحًا. النتيجة أن حفظًا عاديًا لم يعد يستطيع سك `location='approved'` دون بوابة الخرائط. مغطاة في `tests/test_issues_014_020_security.py`.

### ~~ISS-024 — توليد الشريحة لا يشترط وجود اعتماد توليد ولا يطابق المدخلات المرسلة مع الاعتماد~~

**أُصلحت.** `_generation_inputs_guard` أصبح يفشل مغلقًا: أي توليد يعلن `draftId` يتطلب اعتمادًا بحالة `approved` (التسوية تنقله إلى `consumed` فالبوابة لكل تشغيلة)، والمسودة المخزنة يجب أن تطابق `draft_hash` في snapshot الاعتماد، وكل مدخل توليد أرسله العميل يجب أن يطابق نظيره المخزن — الحقول المعاد تشكيلها (`financial_study_model.report`، `land_photos_file_meta`) تُقارن بصورتها المطبعة، والمفاتيح المحذوفة شرعيًا من الحمولة تُتخطى، وplaceholder الصور و`blob:` يُعاملان كقيم غير قابلة للاستهلاك. فشل استعلام البوابة يرجع 503 بدل التمرير، والمسودة خارج نطاق العضو تُرفض 404. مغطاة في `tests/test_issues_014_020_security.py`.

### ~~ISS-025 — فك بعض أقفال العمل يعتمد على booleans يرسلها العميل~~

**أُصلحت.** كل ادعاء سير عمل أصبح يُقرأ من التخزين لا من الحمولة: توليد الخريطة يتحقق من `location_analysis_approved` و`map_approvals` المخزنة في المشروع (مع رفض إعادة توليد خريطة معتمدة واشتراط اعتماد العامة قبل التابعة)، وحفظ المسودة يمرر ادعاءات الاعتماد على `_sanitize_save_workflow_claims` الذي يسقط أي علم بلا قطعة خلفية (مدخلات الموقع الكاملة أو خريطة مولدة مسجلة)، و`slideCheckpoint`/`operation='generation'` لا يفكان القفل إلا بوجود اعتماد توليد حي بحالة `approved` مربوط بالملف نفسه. مغطاة في `tests/test_issues_014_020_security.py`.

### ~~ISS-026 — استعادة إصدار عرض تتجاوز شروط التعديل بعد الاعتماد~~

**أُصلحت.** مسار الاستعادة أصبح يمر بنفس بوابات PUT: قفل دورة حياة المسودة يرفض الاستعادة 423 كما يرفض التعديل، واستعادة ملف معتمد تتطلب صلاحية `post_approval_edit` وسببًا مكتوبًا يُسجل في سجل التغييرات مع تفاصيل النسخة. الاستعادة تبقى revision جديدًا لا إعادة كتابة حرة. مغطاة في `tests/test_issues_014_020_security.py` و`tests/test_presentation_revision_api.py`.

### ~~ISS-027 — مسار الاعتماد القديم ما زال يعدل الحالة بمعايير مختلفة عن البوابة النهائية~~

**أُصلحت.** لم يعد للمسار القديم قدرة موازية على سك اعتماد نهائي: `create_approval` يرفض 409 (`use_final_approval_gate`) أي عرض مربوط بمسودة دخلت دورة الحياة الرسمية أو تستطيع الوصول إلى `final_approval_pending`، فيبقى `final_file_approvals` هو المرجع الوحيد لتلك الملفات. وللملفات التي ما زال الجدول القديم يحكمها، أصبح الطلب يثبت `request_hash` لمحتوى العرض لحظة الإرسال، والقرار يتطلب صفًا pending عبر UPDATE شرطي واحد (سباق قرارين يربح واحدًا فقط)، ورفضًا بلا ملاحظة ممنوع، واعتمادًا على محتوى تغيّر يفشل `content_changed`، وذات-اعتماد الموظف ممنوع ما لم يكن مدير الشركة، وأي قرار على ملف دخل البوابة بعد فتح الطلب يُرفض. مغطاة في `tests/test_issues_014_020_security.py`.

### ~~ISS-028 — قرارات الاعتماد ليست محمية من قرارين متزامنين~~

**أُصلحت.** القرار نفسه أصبح هو الادعاء الذري: `decide_generation_approval` و`decide_final_file_approval` و`decide_section_version` تنفذ `UPDATE ... WHERE id = ? AND status = 'pending'`، فيفشل القرار الخاسر بـ `approval_not_pending`/`version_not_pending` بدل الكتابة فوق الفائز. في اعتماد التوليد يخرج الخاسر قبل إنشاء أي حجز نقاط أو نقل دورة الحياة، فلا يتداخل رفض مع حجز من القرار الآخر. مغطاة باختبارات سباق في `tests/test_issues_014_020_security.py` تحقق أن قرارًا منافسًا يُودع داخل نافذة القراءة-الكتابة يقلب القرار المتأخر إلى رفض ولا يترك حجزًا.

### ~~ISS-029 — المشروع المشترك قابل للفتح لكنه غير قابل للحفظ تحت مستخدم آخر~~

**أُصلحت.** `save_project_draft` و`update_draft_section_statuses` لم تعودا تفلتران بالمالك عندما يُسمى `draft_id` صراحة — المسار أثبت نطاق العضو قبل الوصول إليهما، والفيتو بالمالك كان يحوّل مسودة مشتركة موجودة إلى INSERT يصطدم بالمفتاح الأساسي. الحفظ المصرّح يهبط على صف المالك كما ينبغي. مغطاة في `tests/test_issues_014_020_security.py`.

### ~~ISS-030 — رقم revision للمسودة لا يمنع فقد التعديلات المتزامنة~~

**أُصلحت.** الحفظ يقبل `expectedRevision` ويطابقه مرتين: فحص مبكر يرفض قبل أي كتابة، ثم `UPDATE ... WHERE id = ? AND revision = ?` شرطي يجعل سباق النافذة بين الفحص والكتابة تعارضًا لا كتابة فوق. التعارض يرفع `DraftRevisionConflict` ويترجم إلى 409 (`DRAFT_REVISION_CONFLICT`) مع `currentRevision` بدل إعادة قيم قديمة فوق تعديل أحدث. الواجهة تتتبع `tenantDraftRevision`: تُصفّر عند مشروع جديد، وتُقرأ من `draft.revision` عند فتح مسودة ومن `draftRevision` عند فتح عرض مرتبط، وتُحدَّث من رد الحفظ ومن رد استعادة إصدار قسم، وتُرسل مع كل حفظ بما فيه checkpoint التوليد. مغطاة باختبارات تعارض في `tests/test_issues_014_020_security.py`.

### ~~ISS-031 — رد حفظ متأخر قد يغير معرف المشروع الحالي ويمسح علامة التعديلات غير المحفوظة~~

**أُصلحت.** `saveProjectAsDraftNow` يلتقط مرجع مساحة العمل وعدّاد تحرير يزيد مع كل تعديل قبل إرسال الطلب. الرد المتأخر لا يُطبق `draftId` ولا `revision` ولا يمسح `tenantDraftDirty` ولا يحدّث الشارة إلا إذا بقيت مساحة العمل نفسها بلا تعديل أحدث — فرد مسودة A الواصل بعد الانتقال إلى B لا يبدّل هوية B ولا يدّعي حفظها.

### ~~ISS-032 — فشل حفظ checkpoint يُبتلع وقد يستمر التوليد بلا نقطة استئناف محفوظة~~

**أُصلحت.** `saveProjectAsDraftNow` يرجع `true`/`false` صريحًا: نجاح عند `resp.success`، وفشل عند رفض السيرفر أو الاستثناء أو التعارض. `saveTenantSlideGenerationCheckpoint` يمرر القيمة، فيُظهر توقف التوليد رسالة «تعذر حفظ نقطة الاستئناف» بدل ادعاء الحفظ، وإنشاء الشرائح والملف الكامل يتوقفان قبل التشغيل المدفوع عند فشل الحفظ التمهيدي، وحفظ الخريطة المعاد توليدها يعلن فشله، ونسخة العرض الاحتياطية تُسجل فشلها دون إسقاط مساحة العمل المفتوحة.

## ثالثًا: الفوترة والمفاتيح وربط التوليد

### ~~ISS-033 — دراسة السوق في الخلفية تفقد هوية الشركة~~

**أُصلحت.** `_market_job_worker` أصبح يختم سياق التطبيق بهوية المهمة — `g.tenant_id` بالمستأجر و`g.user_id`/`g.user_name`/`g.user_role` بالممثل الذي أطلقها — تمامًا كما يفعل `require_auth` في مسار الطلب وكما يفعل عامل الكروكي أصلًا. كل قارئ للهوية من `g` (مجلد الرفع في `_store_project_upload`، نشر شعار المنافس كصورة إبداعية، صفوف التدقيق، اختيار مفتاح OpenRouter عبر `usage_ctx`) يرى الآن مستأجر المهمة بدل سياق فارغ يهبط للمفتاح العام. مغطاة باختبار يؤكد أن العامل يرى الهوية المختومة داخل سياقه في `tests/test_issues_014_020_security.py`.

### ~~ISS-034 — التسوية الدورية تستطيع محاسبة تشغيل محجوز مرتين~~

**أُصلحت.** `_unbilled_scope_clause` — الجملة المشتركة بين checkout والتسوية الدورية وعداد الاستهلاك غير المفوتر — تستبعد الآن أي حدث استهلاك مرتبط بمسودة عليها حجز نقاط حي، أو بعرض يغطيه حجز عبر `generation_approvals.presentation_id`. هذا الاستهلاك مدفوع مسبقًا بالضمان وتطالب به التسوية تحت قيد الحجز نفسه، فلم يعد ممكنًا أن يظهر debit من التسوية وdebit من الحجز للتشغيل نفسه. `list_tenants_with_billable_usage` يطبق الاستبعاد نفسه بمرجع مترابط فلا تدخل الشركة دورة الفوترة أصلًا، و`bill_unbilled_usage` يكنس الحجوزات المنتهية قبل الادعاء فلا يبقى حجز ميت يجمّد الاستهلاك إلى الأبد. الاستهلاك خارج نطاق الحجز يُفوتر طبيعيًا أثناء الحجز الحي، والحجز المحرَّر يعيد استهلاكه للقابلية فورًا. مغطاة بثلاثة اختبارات في `tests/test_issues_014_020_security.py`.

### ~~ISS-035 — حجز تشغيل يطالب باستهلاك أوسع من التشغيل وبحالات لم تُسوَّ بعد~~

**أُصلحت.** `_claim_run_usage_tx` أصبح يطابق نفس نطاق الفوترة المحفظية: `package_id IS NULL` فلا تختلط محاسبة الباقة بالمحفظة، و`_AI_BILLABLE_STATUS_CLAUSE` على مطالبة أحداث AI فلا تُقفل صفوف pending بتكلفة مجهولة تحت قيد التسوية — تبقى قابلة للفوترة بعد التسوية السعرية بدل أن يحمل القيد صفرًا ناقصًا، وحد `created_at >= reserved_at` يمنع التهام تحليلات سابقة لحجز مختلف تحت نفس المسودة. الحد نصي بصيغة `YYYY-MM-DD HH:MM:SS` على الجانبين فلا تنزلق صفوف نفس الثانية خارج النافذة، والمطالبة تظل ذرية داخل معاملة التسوية. مغطاة بثلاثة اختبارات في `tests/test_issues_014_020_security.py`.

### ~~ISS-036 — حد مفتاح OpenRouter يخلط السقف التراكمي بالرصيد المتبقي~~

**أُصلحت.** الـ PATCH أصبح يرسل `usage + الاستحقاق المتبقي` لا الاستحقاق وحده: `_openrouter_key_usage_usd` يقرأ الاستهلاك التراكمي المسجل على المفتاح، و`_sync_tenant_credit_to_openrouter` وتحديث الأدمن اليدوي يضيفانه إلى `limit`، فيبقى `limit_remaining` مساويًا لما تحسبه المحفظة فعلًا بدل أن يتقلص بمقدار المحروق عند كل مزامنة. قراءة الاستهلاك عند تعذرها تسقط على القيمة المقصودة نفسها — نقص السقف فشل آمن الاتجاه وتصححه المزامنة التالية، ومفتاح جديد يبقى `limit = الاستحقاق` لأن usage صفر بطبيعته. مغطاة باختبارين في `tests/test_issues_014_020_security.py`.

### ~~ISS-037 — حماية الرصيد لا تغطي كل المسارات المدفوعة وتسمح بالمرور عند أخطاء داخلية~~

**أُصلحت.** حاجز المحفظة أصبح يغطي كل مسار مدفوع يستدعي مزودًا: توليد الصور (`generate-images`، `generate-main-image`، `generate-slide-image`، `generate-image`) والتصور البصري (`visual-concept/generate` و`chat` و`plans-verify` و`prompt`، وفرع `plans-boundary` بمود AI فقط حتى لا تُقفل التعديلات اليدوية المجانية) والمحتوى التنفيذي والوكيل المصمم (`designer-chat` و`training-chat`) وبنّاء الحقول وتحليل المرجع البصري واكتشاف شعار المنافس والمسارات النصية القديمة (`generate` و`official-outline`/`generate-titles`/`generate-outline` و`generate-content` و`ai-edit-slide`/`redesign-slide` و`ai-chat` و`generate-bullets` و`get-image-prompts` و`generate-cover-prompt` و`designer-generate`/`generate-design`). أضيفت مفاتيح تقدير `image_generation` و`visual_concept` و`executive_content` و`designer_chat` و`ai_text` إلى `get_billing_flow_estimates`. والحاجز نفسه لم يعد يفشل مفتوحًا: خطأ قراءة الرصيد تحت التفعيل يرد الآن 503 `BILLING_CHECK_UNAVAILABLE` بدل المرور، وحاجز `REQUIRE_TENANT_OPENROUTER_KEY` يرد `TENANT_KEY_CHECK_FAILED` عند فشل استعلام المفتاح بدل فتح البوابة، بينما يبقى `BILLING_ENFORCE=0` الافتراضي الموثق تعطيلًا صريحًا. مغطاة بأربعة اختبارات في `tests/test_billing_ledger.py`.

### ~~ISS-038 — المطالبات الصغيرة تُعلَّم مدفوعة بعد تقريبها إلى صفر~~

**أُصلحت.** `bill_unbilled_usage` لم يعد يغلق مطالبة تكلفتها المدفوعة تقرب إلى $0.00: عندما يكون `raw_cost > 0` و`billed_amount` يقرّب للصفر تُرجَع المطالبة نفسها (rollback) ويخرج بـ`below_minimum_charge`، فتبقى الصفوف غير مفوترة وتحمل كسور السنت للفاتورة التالية بدل أن تُوسم مدفوعة بصفر للأبد — وهذا هو آلية الحمل نفسها. الصفوف المجانية فعلًا (`raw_cost <= 0`) تُغلق طبيعيًا حتى لا تدور في كل دورة تسوية، وcheckout اليدوي يجيب `billed: False` بالسبب بدل تسجيل قيد صفري، والكنس الدوري يعدّها متخطاة. مغطاة بثلاثة اختبارات في `tests/test_billing_ledger.py` تثبت الحمل، ثم الفوترة المجمعة بعد عبور السنت، ثم إقفال الصفوف المجانية.

### ~~ISS-039 — الباقة المستهلكة تظل مالكة لكل الاستهلاك الجديد بدل الرجوع للمحفظة~~

**أُصلحت.** `_tenant_active_package_id` لم يعد يكتفي بقراءة `tenants.package_id`: يتحقق الآن أن الباقة ما زالت `is_active` وأن `credit_usd - مجموع الاستهلاك الموسوم بها > 0` قبل توسيم صف جديد. باقة نفد رصيدها أو عُطّلت ترجع `NULL` فيسقط الاستهلاك الزائد على المحفظة ويلتقطه checkout والكنس الدوري طبيعيًا بدل أن يظل موسومًا غير قابل للفوترة للأبد. `get_package_remaining_usd` يعامل الباقة المعطلة كمنتهية أيضًا فلا تمنح سقفًا في حساب حد OpenRouter. مغطاة باختبارين في `tests/test_billing_ledger.py` — نفاد الرصيد يعيد التوسيم للمحفظة ويفوتره، والتعطيل يوقف التمليك فورًا.

### ~~ISS-040 — إعادة إسناد نفس الباقة لا تبدأ دورة رصيد جديدة، وتعديل الكتالوج يغير الرصيد القائم~~

**أُصلحت.** الاستحقاق أصبح دوريًا لا تراكميًا على الكتالوج: `_tenant_package_cycle` يقرأ أحدث سطر `tenant_package_history` فيأخذ `credit_usd` من لقطة الإسناد نفسه — تعديل الكتالوج لاحقًا لا يغير ما مُنح فعلًا — و`assigned_at` يحدد بداية دورة الاستهلاك فلا تحسب حروق الدورة السابقة على الإسناد الجديد لنفس الباقة. `_package_cycle_consumed` يجمع الوسوم داخل الدورة فقط بمقارنة نصية مطبعة للثواني، والإسنادات الأقدم من تتبع التاريخ تسقط على رصيد الكتالوج كما كانت. `get_package_remaining_usd` و`_tenant_active_package_id` و`get_client_overview` كلها تعمل على الدورة نفسها فتتفق البوابة والعداد والبطاقة. مغطاة باختبارين في `tests/test_billing_ledger.py` — إعادة إسناد باقة مستهلكة تفتح نافذة جديدة كاملة، وتعديل الكتالوج لا يحرّك استحقاقًا قائمًا.

### ~~ISS-041 — إعادة توليد نفس المشروع تعيد استخدام مهمة الاعتماد السابق~~

**أُصلحت.** مفتاح عدم التكرار أصبح يسمّي التشغيل نفسه: الواجهة ترسل `gen-{approvalId}-{draftId}` بدل `gen-{draftId}` فكل اعتماد جديد يسجّل مهمة جديدة. والتحقق الخادمي مقيد أصلًا بثلاثية `(tenant_id, approval_id, idempotency_key)` عبر `_generation_job_by_idempotency` والفهرس الفريد `ux_generation_jobs_tenant_idem` — مفتاح مكرر تحت اعتماد مختلف أو شركة أخرى طلب مختلف يحصل على مهمة خاصة، وإعادة الإرسال الحقيقية الوحيدة (نفس الشركة والاعتماد والمفتاح) هي ما تسترجع المهمة. كتابة رابط `job_id` في الاعتماد مقيدة كذلك بأن تكون المهمة المرتجعة من نفس الاعتماد. مغطاة باختبارات `test_job_idempotency_key_stays_inside_its_tenant_and_approval` في `tests/test_workflow_gates.py` وبفحص مصدري للمفتاح الجديد في `tests/test_meeting_requirements.py`.

### ~~ISS-042 — حالة انتهاء التوليد وتسوية الأموال مصدرهما العميل، وقد يرجع النجاح رغم فشل التسوية~~

**أُصلحت.** `/settle` أصبح يقابل ادعاء العميل بسجل المهام الخادمي عبر `_approval_generation_jobs`: jobId مسماة يجب أن تخص نفس الاعتماد وإلا `job_mismatch`، واستهلاك الحجز يتطلب مهمة `completed` فعلًا (`job_not_completed`)، وتحرير الحجز ممنوع ومهمة `queued`/`running` ما زالت حية (`job_still_running`)، ووجود مهمة مسجلة دون تسميتها يُرفض بـ`job_required` بدل قيد `unknown-job` — ويبقى مسار بدون مهام مسجلة على سلوكه القديم. و`/finish` لم يعد يبتلع فشل التسوية: نتيجة `settle_generation_approval` تُفحص والخطأ أو الرفض يرد 503 `settlement_failed` مع المهمة بدل `success` كاذب بينما الضمان ما زال حيًا (502 محجوز للبروكسي — فشل يعاد المحاولة يرد 503)، فيمكن إعادة `/settle` لاحقًا. مغطاة بستة اختبارات في `tests/test_issues_014_020_security.py`.

## رابعًا: المهام الخلفية والبريد والاعتمادية

### ISS-043 — حفظ حالة المهمة لا يجعل تنفيذها مستديمًا بعد إعادة تشغيل العامل

**P1 | مؤكدة من الكود، وأثرها مشروط بإعادة التشغيل.**

**الموضع:** `app.py:9772-9785,18023-18035,18348-18359`؛ `designer_chat_reliability.py:1747-1751`؛ `start_server-staging.sh:130-136`؛ `db.py:13279-13313`.

التنفيذ الفعلي daemon threads داخل Gunicorn، بينما ملفات JSON أو generation_jobs تحفظ الحالة فقط. إعادة التشغيل أو تدوير العامل بعد max-requests تقتل العمل غير المنتهي. لم يظهر ربط هذه العمليات بالـjob_queue المستديم أو worker يستأنف payloads منه. النتيجة طلب مدفوع انقطع، وربما ملف حالة running باقٍ دون تنفيذ؛ fallback التخطيط لا يستعيد بقية أنواع المهام.

### ISS-044 — لا يوجد حد مشترك للتزامن أو لطابور العمليات المكلفة

**P2 | مخاطرة تشغيل مشروطة بالحمل.**

**الموضع:** `app.py:913-917,9781-9785,18031-18035,18355-18359`؛ `designer_chat_reliability.py:1747-1751`؛ `assets/js/13-visual.js:639-642`.

كل طلب قد ينشئ thread جديدًا، بما في ذلك مزامنة حدود المفاتيح، ولا توجد سعة طابور أو semaphore عامة/لكل شركة في هذه المسارات. واجهة توليد الشرائح تشغل ثلاث مهام بالتوازي لكل تشغيل، ويمكن لعدة مستخدمين إضافة حمل أكبر. الحد على عدد threads التي تخدم HTTP لا يحد هذه الـthreads المستقلة أو عمليات Chromium.

### ISS-045 — ملف حالة تحليل الأرض يُكتب بطريقة يمكن أن يقرأها عامل آخر ناقصة

**P2 | مخاطرة تزامن واضحة من الكود.**

**الموضع:** `app.py:18267-18296` مقابل الكتابة الذرية في `app.py:17710-17744`؛ `start_server-staging.sh:130-132`.

`_write_land_job` يفتح نفس الملف بـ`w` ثم يكتب مباشرة. القفل process-local ولا يحمي قراءة worker آخر. خلال نافذة التفريغ قد يفشل JSON parse وتُرجع القراءة None، فيظهر `job_not_found` أثناء مهمة سليمة. مسارات السوق والتخطيط تستخدم نشر ملف مؤقت ذريًا بالفعل، لكن تحليل الأرض لم يلحق بها.

### ISS-046 — عامل تحليل الأرض لا يفهم ردود Flask من نوع tuple

**P2 | مؤكدة من الكود.**

**الموضع:** `app.py:18308-18327,18402-18409,18430-18433`.

`_execute_extract_croquis` يعيد في عدة حالات `(jsonify(...), status)`. العامل يستدعي `response.get_json` مباشرة، فيفشل لأن response tuple. بذلك تضيع رسالة مثل نقص الموقع أو كثرة المستندات وfailureReason الأصلي، وتستبدل بخطأ عام `job_failed`. الاختبارات المتزامنة قد تقبل نفس tuple طبيعيًا لأن Flask يتعامل معها نيابة عنها.

### ISS-047 — البريد الذي كان sending عند توقف العملية لا يدخل إعادة المحاولة

**P2 | مؤكدة من الكود، وأثرها مشروط بالتوقف.**

**الموضع:** `db.py:13422-13439,13470-13486`؛ `app.py:14814-14831,14881-14894`.

claim يثبت `sending` ويعمل commit قبل SMTP. لا يحمل السجل lease انتهاء ولا يظهر sweep يعيد sending العالق إلى queued. إذا مات العامل بعد claim، يبقى البريد خارج الاستعلام الذي يلتقط queued فقط؛ وقد يتوقف إرسال دعوات أو إشعارات مهمة. آلية خمس محاولات تعمل عند رجوع SMTP بفشل، لا عند موت العملية.

## خامسًا: الدراسة المالية والمصادر والخرائط والتصدير

### ISS-048 — سنة أول سحب تمويلي تُحسب دائمًا كأنها السنة الأولى

**P1 | مُعاد إنتاجها.**

**الموضع:** `assets/js/09-financial.js:1326-1329`.

حساب minimum لسنة أول سحب يبدأ accumulator بقيمة 1. طالما كل السنوات موجبة فلن يزيد الناتج عن 1 حتى لو كانت أول سنتين بنسبة سحب صفر. الاختبار بخطة يبدأ السحب الحقيقي فيها في السنة الثالثة أعاد السنة الأولى. تتقدم رسوم الترتيب وبداية نافذة الفائدة الثابتة، وقد تنتهي نافذة الفائدة قبل مدتها الصحيحة وتتشوه التدفقات والعائد.

### ISS-049 — نسب المراحل الأقل من 100% تُطبَّق كأنها توزيع كامل

**P2 | مُعاد إنتاجها.**

**الموضع:** `assets/js/09-financial.js:620-635,1269-1272,1309-1316`.

الشاشة تحذر أن جزءًا من نسبة التكلفة/دفعة المطور غير موزع، لكن الحساب يقسم كل نسبة على مجموع النسب المدخلة لا على 100. في الاختبار مرحلة 30% من تكلفة 1000 أخذت 1000، وليس 300، مع بقاء التحذير بوجود 70% غير موزع. عند المجموع صفر يوزع الحساب التكلفة تلقائيًا أيضًا. المعنى المعروض للنسب والتحذيرات لا يطابق القيم الفعلية في التدفق.

### ISS-050 — إعادة توليد خريطة تحذف سجل النسخة العاملة قبل نجاح البديلة

**P1 | مؤكدة من الكود.**

**الموضع:** `app.py:10561-10585`؛ `db.py:7725` وما يليها؛ `maps_service.py:818-828`.

تُحذف صفوف الخريطة ونسخها editable أولًا، ثم يُستدعى المزود. إذا رفض المزود أو انقطع الطلب لا توجد معاملة تعيد السجل القديم. كما أن كتابة ملف map cache نفسها مباشرة وليست نشرًا ذريًا. يمكن فقد المرجع القابل للاكتشاف وإعادة التركيب رغم أن المستخدم لم يحصل على بديل ناجح، خصوصًا في المسودة التي لا تملك snapshot عرض مجمدًا.

### ISS-051 — إسناد المصادر قد ينسب رقمًا لصفحة غير مرتبطة به

**P1 | مخاطرة دقة مثبتة الخوارزمية، لا ادعاء تزوير نتائج موجودة.**

**الموضع:** `market_study.py:911-927,938-944,1480-1511`.

استبدال رابط homepage يختار أول citation أعمق له نفس host دون مطابقة المنافس أو الحقل أو الرقم. عند وجود عدة مشروعات على نفس موقع المطور، قد تُنسب أسعارها جميعًا لصفحة المشروع الأول. تصنيف «مصدر رسمي» يعتمد أيضًا على اسم المصدر أو token من اسم الجهة داخل host، وليس تحقق ملكية فعلي. يعطي ذلك انطباع توثيق أقوى مما تثبته الخوارزمية.

### ISS-052 — المحتوى التنفيذي قد يقبل JSON تالفًا أو إجابة غير مطابقة كنص ناجح

**P2 | مؤكدة من الكود.**

**الموضع:** `app.py:18102-18143`.

إذا فشل `parse_json_object` أو لم يجد parser الحقل الصحيح، يتحول الرد الخام إلى النص المعروض بعد إزالة fences وتنظيف بسيط. يكفي أن يكون غير فارغ ليُعاد success. JSON مبتور أو شرح جانبي أو رد لا يحقق بنية عنصر المخاطر قد يصبح محتوى مشروع بدل خطأ يمنع استبدال النص السليم.

### ISS-053 — عمليات طويلة ما زالت متزامنة وتفوق مهلة الاستضافة المحتملة

**P2 | مخاطرة تشغيل مشروطة بزمن المزود وحجم العرض.**

**الموضع:** `app.py:949,18076-18130,4476-4514,14384-14566`؛ `Dockerfile:18`؛ `start_server-staging.sh:133-134`.

المحتوى التنفيذي يسمح بمحاولات أساسية وبديلة داخل نفس الطلب، مع سقوف كبيرة من tokens، والصور والتصدير يعملان أيضًا ضمن HTTP طويل. مهلة Gunicorn 300 ثانية، وقد تكون مهلة الـproxy أقل. حماية التخطيط والسوق بالـjobs لا تشمل هذه المسارات. قد يستمر الإنفاق أو إنشاء ملف بعد أن يرى العميل فشلًا ويعيد المحاولة.

### ISS-054 — تصدير PDF يفرض 16:9 حتى عندما يكون العرض 4:3

**P2 | مؤكدة من الكود.**

**الموضع:** `slide_engine.py:9336-9337,9531-9532`؛ `assets/js/13-visual.js:1332-1344`؛ `generate_pdf_from_preview.py:831-843,902-903`.

التوليد والمعاينة يدعمان 1280×960 لنسبة 4:3، لكن صفحة PDF والـwrappers والشريحة تُجبر إلى 1280×720 مع overflow hidden. النتيجة قص الجزء السفلي أو اختلاف التخطيط رغم بقاء عدد الصفحات صحيحًا. فحص page count لا يكشف فقد محتوى داخل الصفحة.

### ISS-055 — أبعاد PPTX متغيرات global تتشاركها صادرات متزامنة

**P2 | مخاطرة تزامن واضحة من الكود.**

**الموضع:** `exports/pptx_export.py:40,2802-2809,2865-2881`؛ `Dockerfile:18`.

كل generate_pptx يغير `SLIDE_W_PX` و`SLIDE_H_PX` على مستوى الوحدة ثم يستخدمها داخل حلقة الشرائح. تصدير شركة بنسبة 4:3 بالتوازي مع شركة بنسبة 16:9 يستطيع تغيير قياسات التخطيط في منتصف بناء الملف الآخر، بينما مقاس Presentation نفسه بقي كما حُدد في البداية. التشغيل متعدد threads يجعل هذا تداخلًا واقعيًا.

### ISS-056 — اسم التنزيل يُستخرج من endpoint لا من الملف، ويفقد علامة DRAFT

**P2 | مؤكدة من الكود.**

**الموضع:** `app.py:14497-14499,14551-14554,14662-14670`؛ `assets/js/16-presentations-export.js:684-707`.

الـAPI يعيد `/api/exports/<id>/download?draft=1` أو `/download`. الواجهة تأخذ آخر جزء من URL كاسم الملف، فيصبح `download?draft=1` أو `download` بدل اسم المشروع وامتداد PDF/PPTX. ثم تستخدم blob و`a.download`، فلا تستفيد من Content-Disposition الذي يضيف DRAFT. النتيجة اسم غير صحيح وضياع علامة المسودة الوحيدة في اسم التنزيل.

### ISS-057 — أسماء ملفات التصدير تتصادم عند تشغيلين في نفس الثانية

**P1 | مخاطرة تزامن واضحة من الكود.**

**الموضع:** `app.py:14462-14467,3074-3077`؛ `exports/pptx_export.py:2892-2896`.

اسم الملف يعتمد على عنوان مختصر و`int(time.time())` فقط. عمليتا تصدير لنفس الشركة والعنوان والصيغة في نفس الثانية قد تكتبان نفس المسار، مع وجود سجلَي export يشيران إليه. قد يُستبدل ملف قديم أو يُنزَّل محتوى غير الموافق لسجل الاعتماد، وليس فقط يتكرر الاسم الظاهر.

### ISS-058 — fallback المعاينة يعلن صورًا لا يضمن وجودها، ومسار PDF القديم يعيد رابطًا غير مخدوم

**P2 | مؤكدة من الكود.**

**الموضع:** `assets/js/13-visual.js:1166-1197,1299-1316`؛ `app.py:24193-24201,4131-4148`.

المعاينة تملأ cover و16 moodboard fallback بمسارات stock ثابتة، وقد تنشئ 16 tile لعرض قديم بلا صور. route uploads العام لا يخدم هذه الأسماء إلا إن وجدت داخل maps. كذلك `/api/export-pdf` يعيد `/outputs/<filename>` دون إنشاء export record، بينما لا يوجد route outputs في التطبيق. هذه المسارات القديمة قد تعطي صورًا مكسورة أو نجاح تصدير برابط 404، بخلاف المسار الحديث.

## سادسًا: البناء وقاعدة البيانات والنشر

### ISS-059 — Dockerfile يفشل في البناء النظيف قبل نسخ requirements

**P1 | مؤكدة من الكود؛ لم يُشغل Docker build.**

**الموضع:** `Dockerfile:1-12`.

بعد `WORKDIR /app` يُنفذ `pip install -r requirements.txt` في السطر 10، لكن أول COPY يأتي في السطر 12. صورة Python الأساسية لا تحتوي ملف متطلبات المشروع. لذلك يفشل البناء النظيف بـfile not found قبل الوصول لنسخ التطبيق. هذا عيب ترتيب مباشر وليس مشكلة نسخة مكتبة أو بيانات البيئة المحلية.

### ISS-060 — سياق Docker يمكن أن يضم ملفات محلية حساسة يتجاهلها Git

**P1 | مخاطرة مشروطة بالبناء من checkout محلي.**

**الموضع:** `Dockerfile:12`؛ `.dockerignore:1-30`؛ `.gitignore:37-40,50-52`؛ `population_service.py:14-17`.

`COPY . .` لا يحترم `.gitignore`. قواعد Docker تستبعد `.env` وapp.db فقط من بعض الحالات الحساسة، لكنها لا تستبعد ملف اعتماد BigQuery المتوقع في جذر المشروع، ولا كل قواعد البيانات البديلة وملفات الأدوات المحلية. قد تدخل تلك الملفات في طبقة الصورة عند البناء محليًا. لم يُفتح ملف الاعتماد ولم يُبن أو يُرفع أي image؛ البند يصف خطر سياق البناء نفسه.

### ISS-061 — صورة Docker لا توفر موارد لازمة للتحليل والتصدير حتى بعد تجاوز عيب البناء

**P2 | مؤكدة من إعدادات البناء.**

**الموضع:** `.dockerignore:17-19`؛ `Dockerfile:1-18`؛ `app.py:16311-16315`؛ `generate_pdf_from_preview.py:885-907`.

كل PDF مستبعد من السياق، ومن ضمنه ملف الاشتراطات المحلي الذي يعتمد عليه تحليل الأرض. كما لا يوجد تثبيت Chromium أو مكتبات النظام المطلوبة له في Dockerfile؛ تثبيت حزمة Python Playwright لا ينزل المتصفح تلقائيًا. لذا قد يعمل نفس الكود داخل Docker دون المراجع أو بمحرك PDF fallback أقل دقة، حتى لو عولج ترتيب requirements لاحقًا.

### ISS-062 — إعداد Render يحفظ السجلات في PostgreSQL لكن ملفات العملاء على قرص غير مستديم

**P1 | مخاطرة مشروطة باستخدام Render blueprint الموجود.**

**الموضع:** `render.yaml:5-23`؛ `app.py:19660-19703,14421-14423`؛ `presentation_assets.py:297-324`.

الـblueprint خدمة Docker free ولا يعرّف persistent disk أو object storage. المستندات والصور ومراجعاتها والصادرات تُكتب في ملفات داخل التطبيق. إعادة نشر أو استبدال instance قد تبقي صفوف PostgreSQL بينما تختفي ملفاتها، فينجح فتح سجل المشروع وتفشل صوره ووثائقه وإصداراته. هذا ليس وصفًا لحالة استضافة cPanel الحالية.

### ISS-063 — دعم PostgreSQL يحتوي عوائق صريحة في التهيئة وتحديث الهوية

**P1 | مؤكدة من الكود؛ لم يُختبر PostgreSQL حي.**

**الموضع:** `db.py:795-798,1265,8194-8214`؛ `db_driver.py:177-189,206-243`.

توجد ثلاثة عوائق مستقلة: استعلام `sqlite_master` غير المشروط في `_create_tables` لا يترجمه الـdriver إلى PostgreSQL؛ `update_branding` يكرر cursor مباشرة بينما `PostgresCursor` لا ينفذ iteration؛ و`_ensure_omran_columns` يقرأ `row[1]` من rows تكون dicts على PostgreSQL، فيبتلع الخطأ ولا يضيف الأعمدة المطلوبة. لذلك اجتياز اختبارات SQLite لا يثبت تشغيل التهيئة أو تسجيل الدخول والإعدادات على PostgreSQL.

### ISS-064 — فشل schema/migration يمكن ابتلاعه ويكمل التطبيق بحالة قاعدة ناقصة

**P1 | مؤكدة من الكود.**

**الموضع:** `db_driver.py:104-124,146-157`؛ `db.py:48-91,1796-1819`؛ `deploy-staging.sh:135-136`.

SQLite executescript يطبع أخطاء statements ويواصل، و`init_db` يحول خطأ التهيئة إلى print دون فشل العملية. في migrations متعددة الخطوات قد تُنفذ عمليات لاحقة بعد فشل خطوة سابقة. PostgreSQL execute يعمل rollback للمعاملة كلها عند خطأ، ما يجعل catch-and-continue في callers أخطر. أمر النشر الذي يستدعي init_db قد يخرج ناجحًا رغم توقف إضافة بقية الجداول.

### ISS-065 — النشر غير معزول بين التشغيلات ويمكن أن يخلط ملفات نسختين

**P1 | مخاطرة تزامن وتشغيل مشروطة.**

**الموضع:** `deploy.sh:10-17,28-61,75-90`؛ `deploy-staging.sh:68-71,82-115,131-145`؛ `.github/workflows/deploy-staging.yml:15-29`؛ `app.py:24318-24325,24378-24386`.

production وstaging يشتركان افتراضيًا في REPO_DIR، ويغيرانه بـreset ثم rsync، دون قفل على السيرفر. كل webhook ينشئ عملية خلفية مستقلة، والـworkflow لا يحدد concurrency ويفعل retries لعملية POST. يمكن لنشرين متقاربين تغيير المصدر أثناء نسخه. يُحدّث التطبيق في مكانه قبل إعادة التشغيل، وتُتجاهل أخطاء تثبيت dependencies؛ لا توجد مرحلة release مستقلة أو rollback عند الفشل في هذه السكربتات.

### ISS-066 — سر النشر يُنقل داخل query string ويسجل ضمن عنوان الطلب

**P1 | مخاطرة انكشاف تشغيلي مؤكدة المسار.**

**الموضع:** `.github/workflows/deploy-staging.yml:29`؛ `.github/workflows/deploy.yml:27`؛ `app.py:24280-24282,24340-24342`؛ `start_server-staging.sh:138-139`.

الـworkflow يضع سر webhook في URL، بينما الخادم يقبل header بديلًا أصلًا. عناوين الطلبات قد تدخل access logs وproxy logs وأنظمة المراقبة، وaccess logging مفعل. إخفاء السر في مخرجات GitHub لا يزيله من سجلات الاستضافة. لم تُقرأ أي قيمة سر في هذه المراجعة.

### ISS-067 — تحقق النشر والصحة قد يعلن نسخة ناجحة بينما العامل القديم أو قاعدة معطلة ما زالا يخدمان

**P1 | مؤكدة من الكود، وحدوث الحالة مشروط بالتوقيت أو الفشل.**

**الموضع:** `deploy-staging.sh:138-145`؛ `app.py:24400-24425,24483-24498`؛ `.github/workflows/deploy-staging.yml:39-45`.

يُكتب deployment marker قبل إعادة تشغيل Gunicorn، و`/health` يقرأ الملف عند الطلب ويجيب دائمًا `status: ok` دون فحص DB. العامل القديم يستطيع إذن الإبلاغ عن commit الجديد قبل تحميله. فحص النشر يقبل هذا الحقل، والفحص الآخر يكتفي بـ401 لمسار محمي قبل الوصول إلى قاعدة البيانات. لذلك لا يثبت نجاح التحقق أن النسخة الجديدة تعمل وظيفيًا.

## سابعًا: النسخ الاحتياطي والتحقق الآلي

### ISS-068 — النسخة المسماة full لا تشمل ملفات المشروع اللازمة للاسترجاع

**P1 | مؤكدة من الكود؛ اكتمال حماية الاستضافة الخارجية غير معلوم.**

**الموضع:** `scripts/backup_db.py:50-71,84-110`؛ `app.py:19700-19703`؛ `presentation_assets.py:297-324`.

أداة النسخ تحفظ قاعدة البيانات فقط، حتى مع `--kind full`. DB تحمل مسارات للمستندات والصور والمراجعات والصادرات لا محتواها كلها. استرجاعها دون uploads والأصول المرتبطة لا يعيد نظامًا صالحًا للاستخدام. لا يظهر في الأداة أو سكربتات النشر نسخ منسق لتلك الملفات أو استدعاء backup قبل migrations. قد توجد سياسة استضافة خارجية، لكنها ليست مثبتة هنا.

### ISS-069 — أدوات النسخ قد تستهدف قاعدة مختلفة عن التطبيق

**P1 | مخاطرة مشروطة بموضع الإعدادات.**

**الموضع:** `app.py:32-39`؛ `db.py:16-20`؛ `scripts/backup_db.py:34,50-62`؛ `scripts/restore_check.py:89-94`.

التطبيق يحمل `.env` قبل استيراد DB، لكن أدوات النسخ لا تحملها. تشغيلها من cron دون export للمتغيرات قد يستهدف app.db الافتراضي بدل قاعدة التطبيق. كذلك التطبيق يقدم `DB_PATH` على `DATABASE_URL`، بينما dump يقدم DATABASE_URL إذا كان PostgreSQL. عند وجود الاثنين قد تنسخ الأداة قاعدة مختلفة وتسجل نجاحًا في قاعدة أخرى.

### ISS-070 — علامة restore-tested لا تعني أن استرجاع PostgreSQL جُرب فعلًا

**P2 | مؤكدة من الكود.**

**الموضع:** `scripts/restore_check.py:57-73,83-95`.

فحص PostgreSQL ينفذ `pg_restore --list` ويتأكد أن الفهرس يحتوي TABLE، دون استرجاع قاعدة مؤقتة أو التحقق من المخطط والبيانات والعلاقات. ثم يسجل النسخة restore-tested. فحص SQLite يقيس سلامة الملف، لا توفر الملفات الخارجية أو قابلية تشغيل التطبيق. اسم الحالة في لوحة التشغيل يوحي بضمان أقوى من الاختبار المنفذ.

### ISS-071 — خط النشر لا يشغل الاختبارات، وبعض تغطية الواجهة ومسارات الإنتاج غير مكتملة

**P2 | مؤكدة من الكود.**

**الموضع:** `.github/workflows/deploy-staging.yml:15-83`؛ `.github/workflows/deploy.yml:13-81`؛ `tests/test_postgres_parity.py:26-55`؛ `tests/test_meeting_requirements.py:29-50`؛ `app.py:18018-18022`.

ملفا CI الموجودان ينفذان النشر وchecks محدودة فقط، دون syntax أو unittest أو اختبار Docker. PostgreSQL tests تُتخطى دون DSN. اختبارات عديدة تستخدم المسار المتزامن بدل مسار الخلفية الإنتاجي؛ ونسخة قائمة ملفات الواجهة في suite الاجتماع لا تشمل `19-notifications.js`. نجاح اختبارات الحفظ والتراجع وفحص الصياغة في هذه المراجعة لا يغطي هذه الفجوات، ولا يكشف تلقائيًا مشاكل الصلاحيات والمحاسبة والـworkers المذكورة.

### ISS-072 — صلاحية `support_tickets` تمنح العميل سلطات مكتب الدعم على تذاكره

**P1 | مؤكدة من الكود.**

**الموضع:** `app.py` (`api_update_support_ticket_status`، `_omran_can`)؛ `db.py` (`add_support_message`).

مسار تغيير حالة التذكرة يقبل أي قيمة من `SUPPORT_TICKET_STATUSES` (`open, in_progress, waiting_customer, escalated, resolved, closed, reopened`) لأي حامل صلاحية `support_tickets`، وجلسة الأدمن/الشركة تتخطى فحص الصلاحيات دائمًا — فالعميل يستطيع تعليم تذكرته «تم الحل» أو «قيد المعالجة» أو «مُصعّدة» رغم أن هذه قرارات مكتب دعم المنصة. الاستثناء المقصود كان إغلاق صاحب التذكرة فقط. كذلك رد الأدمن على تذكرته يُسجَّل بـ`author_role='company_admin'` فيُحسب «أول رد من الدعم» (`first_response_at`) ويفسد قياس زمن الاستجابة. النمط العام: صلاحية واحدة تخلط «استخدام قناة الدعم» مع «العمل على مكتب الدعم».

### ~~ISS-073 — ميزة العقود والاتفاقيات: قرارات المنصة بيد العميل~~ — أُغلقت بإزالة الميزة

**P1 | مؤكدة من الكود.**

**الموضع (قبل الإزالة):** `app.py` (`/api/contracts` GET/POST، `/api/admin/tenants/<id>/contracts`، `/versions`، `retention-sweep`)؛ `db.py` (`tenant_contracts`، `tenant_contract_versions`، `enforce_contract_retention`).

العميل كان يكتب `signatureStatus` و`retentionUntil` على سجل عقد بينه وبين المنصة — يستطيع تسجيل عقد «موقّع» ومدّ نافذة احتفاظ بياناته بنفسه، والتفعيل كان يشترط عقدًا نشطًا (`doc:contract`) فصارت دورة حياة الشركة معلقة على سجل يملكه العميل نفسه. القرار: الميزة أُزيلت بالكامل — المسارات، الجدولان، `contract_file` من سجل أنواع الملفات، تبويب العقود في صفحة العمليات ولوحة المنصة، تنبيه `contract_expiry`/`retention_due`، وشرط المستند في بوابة التفعيل (أصبح الاكتمال على حقول الملف القانوني فقط). مايجراشن يسقط الجدولين ويحذف نوع الملف على التثبيتات القائمة، و`test_contracts_feature_is_fully_removed` + `test_contract_routes_are_gone` تمنعان عودته.

## حدود الاستنتاج

هذه البنود تفرق بين خلل مصدر يمكن تحديده، وحالة أُعيد إنتاجها في عزل، ومخاطرة تعتمد على بيئة أو تزامن. لم تُفترض حوادث تسريب فعلية أو خسائر مالية فعلية، ولم تُنسب حالة إعداد سرية إلى الإنتاج. الإعدادات المقصودة للتطوير، والمسارات المتروكة للتوافق، والروابط العامة للأصول ذُكرت كمخاطر في سياق استخدامها المحدد، لا باعتبار وجودها وحده حادثة أمنية.
