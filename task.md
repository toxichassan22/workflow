# تصحيحات ما بعد مراجعة الإنتاج

الحالة: اكتمل التنفيذ والاختبارات، والمتبقي النشر.

## التصحيحات المنفذة

- عاد جدول «4. بنود الإيرادات» إلى PDF الدراسة المالية في مسار الشاشة وHTML ومسار PyMuPDF الاحتياطي.
- يحذف PDF من جدول الإيرادات عمود «مرتبط بمكون» وعمود الإجراءات فقط، ويحافظ على بقية الأعمدة والصفوف وجدول الوصول إلى الإشغال.
- عادت مساحة المنافس بخياري «مساحة ثابتة» و«نطاق من–إلى»، مع حفظ قيم كل وضع وعدم تحويل النطاق الموثق إلى متوسط.
- أصبح زر «استيراد رسمي» يعرض حالة البحث والاستيراد ويمنع الطلب المكرر أثناء التحميل.
- إذا لم يحتو صف المنافس موقعًا رسميًا مثبتًا، يبحث النظام عن موقع رسمي موثق بنتيجة بحث قبل تنزيل الشعار.
- يقبل تنزيل الشعار نطاق الصور الفرعي الرسمي الموثق مع استمرار HTTPS وفحص DNS العام وتثبيت IP وفحص نوع الصورة وحجمها.
- أصبحت تعديلات إحداثيات المعالم واختيار ظهورها تُزامن إلى الجدول وتبقى في حمولة إعادة توليد خريطة المعالم.
- أصبحت خطة الشرائح محدودة بطلب واحد مدته 75 ثانية وبحد 12,000 token على النموذج السريع بدل طلب 600 ثانية الذي يتجاوز مهلة Gunicorn.
- تحمل مهمة الخطة خطة احتياطية جاهزة، ويواصل المتصفح التوليد بها إذا تعطلت المهمة بدل التوقف عند 8% برسالة انتهاء المهلة.

## التحقق

- نجحت الاختبارات المركزة لمسارات الإيرادات والمساحات والشعارات والخرائط وخطة الشرائح.
- نجحت المجموعة الرئيسية: `217` اختبارًا.
- نجحت اختبارات التصدير: `15` اختبارًا.
- نجحت اختبارات عزل مسودات المشاريع: `5` اختبارات.
- نجح فحص Python وفحص JavaScript المستخرج من `index.html`.

## المتبقي

- مراجعة فرق Git، ثم commit وpush إلى `github/main` والتحقق من وصول البناء إلى الإنتاج.
