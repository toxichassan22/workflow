# TodoList — ملاحظات 20 يوليو 2026

> بناءً على ملاحظاتك + فحص الكود الفعلي (`app.py`, `index.html`, `db.py`, `maps_service.py`, `.env`)
> القرارات المتفق عليها موثقة داخل كل بند.
>
> **آخر فحص: 20 يوليو الساعة 1:40 ظهراً** — تم العثور على تعديلات يدوية في `index.html` (679 سطر) + `app.py` (82 سطر) + `maps_service.py` (63 سطر). تم تحديث البنود أدناه حسب الواقع.
>
> [x] = تم تنفيذه فعلياً ، [~] = تم جزئياً / يحتاج مراجعة ، [ ] = لسه.

---

## 0. توضيح سريع: الفرق بين أنواع الدخول (للفهم — مش مشكلة كود)

| التبويب | مين يستخدمه | إيش يشوف بعد الدخول |
|---------|-------------|----------------------|
| **دخول الشركة** | أدمن الشركة (الحساب اللي أنشأ الشركة من "إنشاء شركة") | لوحة الشركة كاملة: إنشاء عروض، إعدادات البراندنق، الحقول المخصصة، إدارة الموظفين، قواعد AI، التدريب، التعميد |
| **دخول الموظف** | موظف أنشأه أدمن الشركة من صفحة "المستخدمين" | فقط: الداشبورد + إنشاء عرض + عرض العروض (حسب الصلاحيات اللي يمنحها الأدمن) |
| **أدمن النظام (SAG)** | ما له تبويب خاص — يدخل من تبويب "دخول الشركة" بحساب `sag@example.com` | لوحة إدارة النظام: إضافة/تعطيل/حذف الشركات |

- ملاحظة تقنية: التبويبين يستدعون نفس الـ API (`/api/auth/login`) — الفرق شكلي فقط، وهذا سبب اللخبطة.

### مهام الدخول
- [x] **L1** — إضافة شرح واضح تحت تبويبات الدخول. *(موجود في `index.html:1089` + كائن `AUTH_TAB_HINTS` يتغير حسب التبويب)*
- [x] **L2** — توجيه تلقائي بعد الدخول: SAG → لوحة الإدارة، أدمن شركة → داشبورد كامل، موظف → داشبورد مبسط. *(موجود فعلاً — `bootstrapTenant()` يوجه SAG للإدارة و`applyRolePermissions()` يخفي البطاقات عن الموظف)*
- [x] **L3** — عرض الدور الحالي في الشريط العلوي. *(موجود كـ `<span id="tenantUserInfo">` يعرض الاسم + الدور: أدمن/موظف/SAG Admin)*

---

## 1. صفحة بناء العرض — إرجاع الأقسام الجانبية ⭐ (أولوية قصوى)

**القرار المتفق عليه: الرجوع لتصميم النسخة القديمة بالضبط** — قائمة جانبية (sidebar) فيها أسماء الأقسام، وكل قسم يفتح لوحده.

> السبب التقني للمشكلة: في تحديث الـ multi-tenant، انستبدل الفورم القديم الغني بفورم ديناميكي مسطح (`tenantProjectForm`) — فاختفت القائمة الجانبية والجداول والحسابات التلقائية.

### الأقسام المطلوب إرجاعها (من النسخة القديمة — commit `44a2516`)
- [~] **S1 — بيانات المشروع**: اسم المشروع ✔، نوع المشروع ✔، الموقع التفصيلي ✔، المدينة ✘، فكرة المشروع ✘، هيكلة المشروع ✘، اسم الجهة/المطور ✘، شعار إضافي ✘، الصورة الرئيسية ✘. *(الأولين موجودين من الحقول الديناميكية؛ باقي الحقول القديمة لم تُرجع)*
- [~] **S2 — الموقع والمميزات**: رابط قوقل ماب ✔، مميزات المشروع ✔ (حقل `project_features`)، فرص الاستثمار ✔ (حقل `investment_opportunities`)، مميزات الموقع ✘ (ما وُجد حقل منفصل `location_features`).
- [x] **S3 — المساحات والعناصر**: مساحة الأرض، نسبة البناء %، ملاحظة المساحات + **جدول المكونات** مع الحسابات التلقائية للمساحات والإيرادات. *(موجود في `index.html:2669`)*
- [x] **S4 — الافتراضات المالية**: جميع الحقول + **4 مؤشرات محسوبة تلقائياً**. *(موجود في `index.html:2756`)*
- [x] **S5 — الجدول الزمني**: سنة البداية، عدد السنوات، عنوان الشريحة، عنوان فرعي + **جدول التوزيع الزمني** مع مراحل افتراضية. *(موجود في `index.html:2832`)*

### الأقسام الجديدة المطلوب إضافتها
- [x] **S6 — تحليل SWOT**: نقاط القوة/الضعف/الفرص/التحديات. *(حقول `swot_*` موجودة في قاعدة البيانات وتنعرض داخل السايدبار)*
- [~] **S7 — الموقع والخرائط**: عنوان الموقع ✔، رابط قوقل ماب ✔، إحداثيات lat/lng ✔، رقم المخطط/القطعة ✔، مساحة البناء ✔، نظام البناء ✔، البنية التحتية ✔، الطرق ✔، المعالم القريبة ✔، نطاق التأثير ✔، **اختيار نوع الخريطة في صفحة البيانات ✘** (ما زال متاح فقط في صفحة المعاينة).

### متطلبات تقنية للفورم الجديد
- [x] **S8** — القائمة الجانبية (sidebar) بأسماء الأقسام + التنقل + مؤشر القسم النشط عبر IntersectionObserver. *(موجود في `index.html:1437` + `populateProjectSidebar`)*
- [x] **S9** — أزرار السايدبار: "حفظ كمسودة" + "التالي: اقتراح خطة الشرائح" + "تعبئة بيانات تجريبية" + "حذف المشروع". *(موجودة في `index.html:1439`)*
- [x] **S10** — دمج الحقول الديناميكية مع الأقسام الثابتة. *(الكود يعرض حقول DB المجمعة حسب `section_key` ثم يضيف جداول المكونات/الماليات/الجدول الزمني)*
- [x] **S11** — الحفاظ على صلاحيات الأقسام للموظفين (`tenantAllowedFieldSections` تتحقق قبل إضافة القسم للسايدبار).
- [~] **S12** — زر "تعبئة بيانات تجريبية" يعبّي الحقول النصية + جدول المكونات + الحسابات المالية تلقائياً؛ **جدول الجدول الزمني يُعاد حسابه لكن ما يتعبأ من `sample` بشكل صريح** (يعتمد على المراحل الداخلية فقط).

---

## 2. الخريطة — دعم رابط قوقل ماب ⭐ (أساسي — طلب صريح)

**المشكلة الظاهرة في السكرين شوت**: لصق رابط `https://maps.app.goo.gl/...` في حقل "عنوان الموقع" يرجع خطأ `Geocoding API error: ZERO_RESULTS` — لأن Geocoding API يفهم العناوين النصية فقط، مش الروابط.

- [x] **M1** — حقل "رابط الموقع على قوقل ماب" مضاف داخل قسم الموقع (`location_maps_link`).
- [x] **M2** — دالة `extract_coords_from_maps_link()` في `maps_service.py` تدعم الروابط المختصرة (`maps.app.goo.gl`) وتستخرج `@lat,lng` و `!3d...!4d...` و `q=...` و `center=...`.
- [x] **M3** — fallback: لو الرابط ما أعطى إحداثيات، يرجع للعنوان النصي عبر `geocode_address`.
- [x] **M4** — حقول lat/lng تتعبأ تلقائياً في الفورم بعد نجاح الاستخراج (`geocodeProjectAddress` في `index.html:3008`).
- [x] **M5** — رسائل خطأ عربية بدل `ZERO_RESULTS`: "ما قدرنا نحدد الموقع من هذا الرابط — جربي لصق رابط قوقل ماب مباشر أو كتابة العنوان النصي".

---

## 3. توليد الصور — الخطأ الحقيقي مش الرصيد ⭐

**التشخيص من الكود واللوقات**:
- توليد الصور كلها (الغلاف + المودبورد + شرائح الديزاينر) يمر عبر **OpenRouter** — مش Gemini مباشرة ومش z.ai.
- الخطأ `401 "User not found"` طلع من لوقات قديمة (3 يوليو) — قبل تحديث المفتاح في 14 يوليو.
- رصيد z.ai يُستخدم للنصوص فقط (GLM) وهو شغال. رصيد Gemini المباشر **ما يُستخدم أصلاً** في الكود الحالي.

**تحديث 20/7 — فحص مباشر على OpenRouter API:**

- [x] **AI1** — المفتاح الحالي في `.env` **صالح وشغال** — ما يحتاج مفتاح جديد. *(تم التحقق مباشرة من OpenRouter API — وموديل الصور `gemini-3.1-flash-image-preview` متوفر)*
- [x] **AI2** — الرصيد موجود لكن **قليل: باقي ~$4.26 فقط من $126** — يكفي لتيست محدود، يفضل شحن الحساب قبل توليد عروض كاملة بالصور.
- [ ] **AI3** — اختبار فعلي: توليد صورة الغلاف + المودبورد + شرائح كاملة (المفتاح جاهز — الرصيد ~$4.26).
- [~] **AI4** — رسائل الخطأ العربية مضافة في السيرفر (`app.py`) لحالات: مفتاح مفقود، رصيد غير كافٍ (402)، تجاوز الحد (429)، وتعذر الاتصال. *(الواجهة الأمامية تعرض الرسالة مباشرة — ممكن تحسينها لتكون أشمل).*

---

## 4. تحسينات عامة على الواجهة ("المنصة ما تكون كدا")

- [x] **U1** — قائمة منسدلة "الإدارة" في الشريط العلوي تحتوي على إعدادات الشركة/الحقول/الموظفون/قواعد AI/التعميد/لوحة المدير. *(موجودة في `index.html:1144`)*
- [ ] **U2** — توحيد شكل الصفحات (الداشبورد، الإعدادات، المستخدمين) بتصميم متناسق.
- [x] **U3** — دليل استخدام سريع (onboarding) يظهر لأول مرة للمستخدم (`onboardingGuide` يظهر إذا لم يُخزن `onboardingSeen`).
- [~] **U4** — رسائل خطأ عربية محسّنة في `/api/generate-*` للصور وفي `/api/geocode` للخرائط؛ تبقى بعض الرسائل العامة في النظام تحتاج مراجعة.

### خطوات استخدام المنصة حالياً (مرجع سريع لك)
1. دخول الشركة → الداشبورد.
2. "عرض تقديمي جديد" → تعبئة بيانات المشروع → "التالي: اقتراح خطة الشرائح".
3. مراجعة/تعديل خطة الشرائح → "التالي: توليد الصورة الرئيسية".
4. توليد صورة الغلاف → المودبورد → توليد الشرائح كاملة.
5. معاينة/تعديل الشرائح → تصدير PDF أو PPTX.

---

## 5. أسئلة مفتوحة لاجتماع بكرة الصبح

1. الحقول المالية القديمة (الجداول والحسابات) — هل تدخل في توليد الشرائح بنفس الطريقة القديمة (جداول مالية جاهزة في العرض) أم يعيد الـ AI صياغتها؟
2. جدول المكونات وجدول الجدول الزمني — هل يحتاجون تخصيص لكل شركة (أعمدة مختلفة) أم شكل ثابت للجميع؟
3. نوع الخريطة الافتراضي لكل شركة — يتحدد من إعدادات البراندنق؟
4. باقي ملاحظاتك اللي ذكرتي إنها كثيرة — نمشي عليها بند بند في الاجتماع ونضيفها هنا.

---

## 6. ترتيب التنفيذ المقترح

| الترتيب | البند | السبب |
|---------|-------|--------|
| 1 | **AI3** (تيست توليد الصور) | المفتاح شغال والموديل متاح — الرصيد ~$4.26 |
| 2 | **U2** (توحيد شكل الصفحات) + **S1/S2/S7** المعلّمة بـ [~] | إكمال الحقول القديمة المفقودة + اختيار نوع الخريطة في صفحة البيانات |
| 3 | **U4** (مراجعة رسائل الأخطاء) | بعد جمع ملاحظاتك الكاملة |
| 4 | **S12** (تعبئة بيانات تجريبية كاملة للجدول الزمني) | سريع |

---

*آخر تحديث: 20 يوليو 2026 — الساعة 1:40 ظهراً — تم العثور على تعديلات يدوية واسعة في `index.html` و`app.py` و`maps_service.py`. معظم بنود الفورم والخريطة والدخول تمت؛ تبقى بعض الحقول القديمة المفقودة (المدينة/المطور/الفكرة/الشعار/الصورة) وتيست توليد الصور.*
